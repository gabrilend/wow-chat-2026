# Conversation Summary: 3c438d5e-40a6-4a2c-b560-eefe612e9fbf

Generated on: 2026-09-23 23:59:37
Models: claude-opus-5-5

--------------------------------------------------------------------------------

### User Request 1

Hi, can you give me a summary of the profile options in this project?

--------------------------------------------------------------------------------

### Assistant Response 1

There are four profiles you can build today, plus a fifth (explore) that is
planned but not built yet. The active one is **vanilla**, stored in the
`.profile` file. All four build from the same kind of setup: one of two cloned
server source trees, a list of source-code patches applied before compiling, a
list of setup steps run after compiling, and config-file changes chosen by
profile name.

| | **release** | **beta** | **alpha** | **vanilla** (active) |
|---|---|---|---|---|
| Source tree | `source-beta` | `source-beta` | `source-alpha` | `source-beta` |
| Max level | 20 | 20 | 80 | 40 |
| Starting level | 1 | 1 | 40 | 20 |
| XP rate | 2× | 2× | default | default |
| Lua scripting | engine compiled in, no project scripts linked | the wow-chat design scripts (`src/lua-beta/`) | none | vanilla-only scripts (`src/lua-vanilla/`) |
| Source patches | 17: compile fixes, bot-crash guards, symmetric aggro | 24: everything, including the Lua-engine feature patches and wow-chat gameplay changes | 2: minimal compatibility | 19: compile fixes, Lua-engine stability, bot starter kit |
| After-compile setup | logs and configs only | Lua scripts, death knight class system, destructive creature and trainer changes, class-selector NPCs, empty loot chests | logs and configs only | starting zones, flight paths removed, starter gear, pre-trained abilities, Classic 40/60 riding level gates, starting professions at skill 125, no intro cinematic |
| Profile-only config | — | instant teleport | — | death knight disabled, bot level cap, bot population and account count, custom spells |

Every profile gets these config changes: database connections, directory paths,
80% run speed, the GM login state, network ports, realm list, realm ID and the
SOAP console. Release, beta and vanilla also get 10× fall damage and caps tied
to character level.

**One conflict to check:** `CLAUDE.md` says release caps at level 80, like
alpha. The config patches say otherwise: the level-20 cap and level-1 start
apply to both beta and release, and only alpha gets 80. In practice release is
currently beta without the wow-chat gameplay patches and scripts. Which is
intended: should release be 80, or is `CLAUDE.md` out of date?

**Explore (planned, issue files not committed):** a profile with no combat. The
world is emptied to critters only, and critter density is tripled. It spawns
herbalism and mining trainers, and adds rare naturalist companion bots. They
describe plants and minerals in plain language, grounded in generated images of
the thing they are looking at. None of it is implemented yet.

--------------------------------------------------------------------------------

### User Request 2

can we make a new profile that's called "plain" which is the vanilla profile
except the max level is 60 and the starting level is 1 and the starting gear
patches are not applied along with other similar patches like pre-trained
abilities and such. You can consult me if something seems equally plausible. The
adjusted riding gates is preferred. No need for starting professions at level
125. Idk what the starting zone patch is. I'd like to know about the death
knight class system, but idk if we have it prepared yet. First up on the
development agenda is custom classes, but the default game level 1-60 with
playerbots and quality of life adjustments should be the initial baseline
standard that we develop for.

--------------------------------------------------------------------------------

### Assistant Response 2

> I've mapped every place the vanilla profile is wired in. Before I write the
> issue file, I need your call on a few settings where either answer is
> reasonable.

**[Asked the user]**

*Q1 — Flights:* Vanilla removes every flight path in the world (flightmasters
give flavor lines instead). What should plain do?
- Keep flight paths (Recommended) — Stock game behavior. Plain is meant to be
  the default 1-60 baseline, and removing flight paths is a vanilla design
  choice (148i), not a quality-of-life change.
- Remove, like vanilla — Copy vanilla's flight-path removal and flavor text
  into plain's world database.
→ **Selected:** Remove, like vanilla

*Q2 — Movement:* Two movement tunings come from wow-chat's design: 10× fall
damage (on vanilla/beta/release) and 80% run speed (currently on every profile).
What should plain do?
- Both stock (Recommended) — Normal fall damage and 100% run speed. This fits
  'the default game', and means narrowing the run-speed patch, which currently
  applies to every profile.
- Keep both, like vanilla — 10× fall damage and 80% run speed.
- Stock fall, 80% run — Leave the all-profile run-speed patch alone and only
  drop the fall-damage multiplier.
→ **Selected:** Keep both, like vanilla

*Q3 — Death knight:* Death knights: stock WotLK starts them at level 55 in
their own scripted zone (Acherus). With a cap of 60 they would have 5 levels to
play. Vanilla disables them. Beta has a separate 'DK from level 1' system, but
it only has stats and trainers up to level 20, so it doesn't fit a 1-60 profile.
What should plain do?
- Disable DKs (Recommended) — Same class-creation block as vanilla. Custom
  classes are next on the agenda, and DK can be revisited then.
- Allow stock DKs — DKs start at 55 in Acherus, as in the stock game, and
  reach the cap quickly.
→ **Answered:** allow stock DKs, but there are no bot DKs allowed and level 55
is required. When you create a DK it deletes that level 55 character that you
leveled to that level. This is the replacement mechanic. It might need a special
custom NPC or something, with newly created DKs who don't choose being unable to
leave acherus until they choose. Alternatively, we can just skip the DK intro
zone and teach them all the abilities they would have gained from quests at the
level when they should have gained it. The DK starting zone becomes a normal
leveling zone, with whichever area being unlocked (within a level range) is
randomized. So for example the first area will always be pretty easy because it
needs low level noobs to learn there (level 55) but the later parts like in the
city can be low level coded. And the parts by the chapel and the distant docks
can be starting areas that can be flight path flown to. But each one is
different at different times, they cycle around in the most fair way possible. I
can describe better if you don't have ideas, especially with some context
provided about how the level zones are distributed in terms of phases and
geographic location in the DK starting zone.

*Q4 — Cinematic:* Vanilla skips the race intro cinematic on first login. Keep
that for plain?
- Skip it (Recommended) — A small quality-of-life change that doesn't affect
  gameplay.
- Play it, stock — Race intros play on first login, as in the stock game.
→ **Selected:** Skip it (Recommended)

--------------------------------------------------------------------------------

### User Request 3

Base directory for this skill: /home/ritz/.claude/skills/issue-lifecycle

# Issue lifecycle

The owner's CLAUDE.md is the authority on what issue files are for and what
they must contain; where it and this skill ever disagree, CLAUDE.md wins. This
skill adds the mechanics CLAUDE.md leaves open: which tools answer which
question, in what order, and what to do when a tool says no.

All tools live in one place and are called by absolute path:

| Tool | Answers |
|---|---|
| `/home/ritz/programming/ai-stuff/scripts/validate-issues <project>` | is the issue tree consistent; next free number per phase |
| `validate-issues <project> --next <phase>` | the number to give a new issue |
| `validate-issues <project> --file <issue>` | is this one issue well-formed and are its links matched |
| `/home/ritz/programming/ai-stuff/scripts/progress-dashboard.lua <project> -m` | done/open counts per phase (for progress files and demos) |
| `/home/ritz/programming/ai-stuff/scripts/commit-own-changes <repo> -F -` | commits exactly the lines this session wrote, plus the changed transcripts of the commit's projects (any conversation's), and runs the repository's commit hooks, without touching the shared staging area |
| `/home/ritz/programming/ai-stuff/scripts/stage-own-changes <repo>` | previews what that commit would take; writes nothing |
| `/home/ritz/programming/ai-stuff/scripts/claim-own-change <file>` | records lines this session changed by a route the edit ledger cannot see (see Committing) |

Each tool has a `.info.md` beside it; read that rather than the source.

## Before writing a new issue

1. **Search first, including `issues/completed/`.** Grep the issue tree for
   the feature's nouns, not just the likely title. A completed issue about the
   same machinery is reopened and extended rather than duplicated: history is
   more useful stacked vertically in one file than spread across several.
2. **Pick the phase by what the work builds on**, not by when it is being
   done. Lower numbers are foundations; later issues depend on earlier ones.
3. **Take the number from the tool:** `validate-issues <project> --next
   <phase>`.
   It reads the project's own naming shape (522, 1001, 9-007, A04) and never
   reuses a number that a completed file already holds.
4. **Write the blueprint** with the three sections CLAUDE.md requires, plus
   link fields in the shape the project already uses (look at a neighbour).
   Name related functions, structures and files instead of pasting code.
5. **Check it:** `validate-issues <project> --file <new-issue>`, and fix what it
   reports on the new file. If the new issue blocks or is blocked by others,
   add the matching line to the other side too.

## While working

- Keep **Current Behavior** true. It is the only section that changes while
  an issue is in progress; rewrite it in place rather than appending a log.
- A question that surfaces is written into the issue (an "Open questions"
  section) and then asked. An issue with an unanswered question is in
  progress, not done.
- Anything discovered that changes another issue's assumptions is written into
  that issue now, not at completion.

## Splitting into sub-issues

Split when an issue holds work streams that could be built, tested or reviewed
separately, or when it cannot be finished in one sitting. Do not split an issue
that is already one mechanism. When splitting, produce and then write out:

1. **A table** — `| ID | Name | Dependencies | Description |`, where ID is the
   parent number plus a lower-case letter (103a, 103b), Name is dash-separated
   lower-case words, Dependencies is "None" or sibling IDs.
2. **The rationale** — the distinct work streams, why one issue cannot hold
   them, what splitting buys.
3. **The execution order** — a small dependency graph, e.g.
   `103a (foundation) → 103b (needs 103a) → 103c (parallel with 103b)`.

Each row becomes a file `{parent}{letter}-{name}.md` with the three sections.
The parent keeps its blueprint and gains a short list of its sub-issues; the
analysis itself is not appended to the parent (that would be worklog, not
blueprint). Run `validate-issues` afterwards: it reports orphaned sub-issues
and one-sided links.

## Completing an issue

An issue is complete only when nothing in it is deferred and no open question
is unanswered. Then, in this order:

1. **Run the project's tests.** A fixed bug gets a test that would have caught
   it.
2. **Rewrite the issue as the blueprint of what was built**: Current Behavior
   states the built system; steps name the real functions and files; decisions
   not taken are stated with their reason. Poetry the owner wrote during the
   work goes into the issue verbatim.
3. **Move it** into `issues/completed/` (`git mv`, so the history follows).
4. **Update `issues/phase-<N>-progress.md`** for that phase. Take counts from
   `progress-dashboard.lua <project> -m`, or better, name the command instead
   of copying numbers that will go stale.
5. **Update related issues** whose Current Behavior or assumptions changed.
6. **Run `validate-issues <project>`** and fix new findings that the change
   caused. Findings older than the change are reported to the owner, not
   silently fixed in passing.
7. **Commit** as below.

## Committing

The rule (CLAUDE.md): a commit carries exactly the lines this session wrote —
never another session's work, even if it sits in the same file — and every
changed transcript of the commit's projects rides along — the session's own
project folder and the projects of the files it commits — this
conversation's and any other's left behind (a session that committed, talked
on and quit), so each project's story in git stays whole.

Commit small and often: each piece as soon as it is done and checked, not a
pile at the end.

1. Optionally preview: `stage-own-changes <repo>` lists what would be taken
   and what left out, and writes nothing.
2. Commit: `commit-own-changes <repo> -F -` with the message on standard
   input. It builds the commit on a private staging list from this session's
   own lines (the edit ledger the edit hook keeps) plus those transcripts, and
   never touches the shared staging area, so nothing another
   session has staged can ride along. Plain `git commit` is refused by the
   commit gate.
3. Message style: say what the software now does, in plain English and by
   mechanism or analogy; mention any extra changes and why. No function names.

**When it stops or leaves something out.**

- **A tangled ("mixed") block** means another session changed lines right
  next to or on top of yours. Nothing is committed. Tell the owner which file
  and line; decide together what that region should say. `--leave-mixed`
  commits your other blocks meanwhile.
- **Lines that are yours but were written by a route the ledger could not see**
  (a script you ran, a generator, `sed`, a rename): record them with
  `claim-own-change <file>` and say so in the commit message.
- **A warning about the shared staging area** means someone had staged that
  file by hand in a way that overlaps your commit; their entry was left alone.
  Tell the owner.

The one-time permission tokens (`/tmp/claude-allow-*`) belong to the person.
Never create one; if a gate names a token, relay the gate's message and let
the owner decide.

## Open questions

- Phase ≥ 10 in compact names depends on the project's issue-number width;
  `validate-issues` decides it from evidence (progress files, `phase-N-demo`
  names). A project with none of those gets a warning. Should the house rule
  fix one width (two digits, as in the 522 example) for every new project?

--------------------------------------------------------------------------------

### Assistant Response 3

> Research is done. Writing the issue files now: one parent issue for the plain
> profile and four sub-issues.

> Moving the death knight work out of the plain profile. It becomes a standalone
> phase-7 issue (718) that waits on the custom-class infrastructure, and plain
> gets an interim death knight rule.

I've written the issue files for the plain profile. None of the code is written
yet, because four open questions affect it.

**The plain profile (issue 155), split into three parts:**
- **155a, making the name work everywhere:** about 15 separate places look a
  profile name up, and one wrong gate fails silently with stock values. Plain
  gets its own databases (`acore_*_plain`), realm id 5, and the same source
  tree, modules and ports as vanilla. Its databases start from a fresh
  AzerothCore base rather than a copy of vanilla's, because vanilla's already
  holds the starter kit, pretrained abilities and moved spawn points.
- **155b, config settings, decided one by one:**
  - a new level-60 cap;
  - start at level 1;
  - bots start at 1 and level up to 60 on both continents;
  - 10× fall damage and 80% run speed;
  - the same bot population as vanilla;
  - no pretrained spells.
- **155c, database and source patches:** plain keeps flight-path removal, the
  skipped intro cinematic and the riding gates. Those three files contain
  nothing specific to vanilla, so plain links to them instead of copying them,
  and a tuning change reaches both profiles. Plain skips the starter kit,
  pretraining, starting professions and bots in starter gear.
  - Riding gates at a cap of 60 work out to exactly the Classic arrangement: the
    60% mount at level 40 and the 100% mount at the level cap.

**The starting zone patch:** it moves each race's spawn point from its level-1
starting valley (Northshire, Valley of Trials) to a level-20 town somewhere else
in the world (Menethil Harbor, Ratchet, Astranaar, Tarren Mill). Plain starts at
level 1, so it keeps the stock valleys.

**The death knight system in beta:** death knights start at level 1, still in
their starting zone, Acherus. A script auto-completes the exit quests so they
can leave straight away, and custom trainers teach a reduced ability set from
level 2 to 20. It only covers levels 1–20, so it doesn't fit plain.
- It also shows that the start location is set by the server. The vanilla issue
  that disabled death knights said a client patch would be needed; that premise
  looks wrong.

**Your death knight design is now issue 718,** in phase 7 and waiting on the
custom-class infrastructure, as you asked. Your text is quoted verbatim, and I
added the Acherus context you asked for, taken from the server's data:
- **Quest levels:** all 59 Acherus quests are level 55, so no level gradient
  exists; your regional level bands would be the first one.
- **Phases:** quest completions switch the zone through six stages, from
  breaking the Scarlet line to the burning of New Avalon.
- **Abilities:** four quests grant a spell directly: Runeforging, the
  Deathcharger mount, Death Gate and one reward still to identify. Other
  abilities are introduced by the questline's scripts and need a closer read.
- **Geography:** the landing point → Havenshire → Light's Point → New
  Avalon → Chapel of the Crimson Flame → King's Harbor docks.

**Questions that affect building plain now:**
1. **Death knights until 718 exists:** disable them like vanilla, or allow stock
   death knights with the two restrictions the config already supports? Those
   are "an account needs a level-55 character" and "no bot death knights".
2. **XP rate:** I've assumed 1× like vanilla; beta and release use 2×. Is 1×
   right?
3. **Outland:** the Dark Portal opens at level 58, so plain characters at
   58–60 can walk into Hellfire Peninsula. Close the portal, or leave Outland
   open as content for the level cap?
4. **Naming:** the pending rename issue (152) would rename vanilla to "basic",
   which is very close in meaning to "plain". Is that intended, or should one of
   them change?

718 has more questions of its own (which issues count as the infrastructure, the
chooser NPC versus skipping the intro, deleting versus retiring the level-55
character, what drives the rotation), but they can wait until that issue comes
up.

I haven't committed anything; per your rules, commits happen when an issue is
completed.

--------------------------------------------------------------------------------

### User Request 4

the custom class architecture if I remember correctly was just assigning certain
class's abilities to a middle-ground class. They'd visit the other trainers, and
they could be a warrior mage. Or a necromancer druid. Whatever they'd like. Some
affordances are made, like converting resource cost (like rage and energy) to
mana or vice-versa, but they are meant to be normalized. Rage and energy
characters have more staying power over mana characters, who either have to
invest in regeneration or accept that they are sprinters who run out of mana.
But we can normalize their ability costs by thinking about how that class would
use that type of ability, and match it to other similars in player cast tempo
cadence.

we should have characters spawn at a random starting zone. Fully randomized.
But, it's syncronized, so like the next 30 alliance characters spawn in
northshire and the next 30 horde characters spawn in Mulgore, then after that
the next 30 alliance characters spawn in teldrassil and the next 30 horde
characters spawn in Eversong Woods, etc. Make sure to ensure that quests aren't
gated by race anymore, only faction.

let's leave Outland as content available at level cap. That way we can get raid
quality gear from questing.

let's name it basic/expert, with this being the "basic" tier and "expert" being
level 61-80 where you start at 60 but it's not built yet.

--------------------------------------------------------------------------------

### Assistant Response 4

> Updating the basic parent issue with your answers.

> Adding 155f and the accuracy patch to the parent issue, then writing the
> expert stub and revising the old rename issue.

> Adding basic to the startup scripts. Next are the repository, branch, pinned
> commit and module tables.

> Now the Edit calls for these small blocks.

> Creating basic's Lua directory and SQL directories.

> Plumbing is done. Now the config patches for 155b.

> Now the bot progression and death-knight config patches.

> Writing a test that runs every config patch for basic against a scratch copy
> of the config files and checks each value. It is offline and never starts the
> server.

> All 20 config checks pass for basic. Since the run-speed key turned out not to
> exist, I'm checking every other key the config patches edit for the same
> problem.

> Adding the key-existence check to the test.

> Config gates done; the test passes 20 of 20 and finds no misspelled keys. Next
> is 155c, the patch lists. First, checking that the accuracy patch still
> matches the current source code.

> Writing the source patch now, in the same shape as the existing B025 patch.

> That insert section came out convoluted. Simplifying it before testing.

> Worth making this a reusable test: copy the source tree into RAM, apply
> basic's whole source-patch list, check every return code, then revert and
> confirm the copy matches the original byte for byte.

> The revert leaves behind one blank line in the header. Fixing the undo step so
> it removes that line too.

> Next: flavor lines for the Outland flightmasters. Outland is open on basic,
> and the vanilla flight-path file left those NPCs silent. Pulling the list of
> Outland flightmasters from the stock data.

> Running basic's database setup steps against a scratch copy of the SQL
> directories. MySQL is down, so this checks the file handling and the "not
> registered" notice.

> 155f is written: the SQL, its revert, and its setup step. Now updating the
> issue files so their "current behavior" sections describe what was built, then
> the docs.

> Recording your custom-class description verbatim in the custom-class issue
> (705) and in the death knight issue (718), and noting where it differs from
> the design those issues describe.

> Useful finding: the custom-class Lua code exists but is switched off. It lives
> in the beta Lua directory as `custom-classes.lua.disabled`, which basic
> doesn't load. Correcting the stale path in 705.

> Now updating each issue's "current behavior" section to describe what was
> built.

> Writing the post-install checker (step 4 of 155c, extended to cover 155d–f)
> so it's ready for your install run. Looking at the vanilla one for its shape
> first.

> Writing the user-facing page for the basic profile. The vanilla page is also
> out of date: it still describes the old two-town starting zones.

> `claim-own-change` may have claimed another session's uncommitted line in the
> table of contents along with mine. Checking how the tool works before any
> commit.

> Those two findings predate today's work and I've left them alone. Final re-run
> of both offline tests and a syntax check of every script I touched.

The basic profile is built and ready for you to compile. Nothing has been
compiled, installed or run, and nothing is committed. Your rule is to commit
when an issue is complete, and every basic issue still has open questions
(listed at the end). The death knight issue (718) waits on the custom-class
work, as you asked.

## What basic has now
- **Profile name:** the scripts, config patches, database-name helper, realm
  list and verify step all know `basic`. It has its own databases
  (`acore_*_basic`), realm 5, and new `src/lua-basic/` and `sql/basic/`
  directories.
- **Config:** level cap 60, start at 1, bots level 1→60 across Eastern
  Kingdoms, Kalimdor and Outland, 10× fall damage, 80% run speed. Death knights
  are allowed, but an account needs a level-55 character and no bots play them.
- **Patch lists:** vanilla's list without the starter kit, plus the accuracy cap
  (B005). Flight-path removal, Classic riding levels (60% mount at 40, 100% at
  60) and the skipped intro reuse vanilla's files through links.
- **Outland flightmasters:** the 35 on the Outland map now have flavor lines.
  That includes six in the Draenei and Blood Elf home zones, so vanilla gets
  them too. The Outland lines don't name each NPC's outpost, because I couldn't
  confirm which one each serves.
- **Rotating starting valleys (155d):** a new source patch (B028) hooks
  character creation. Each faction's current valley changes every 30 characters
  in a shuffled order that never repeats a valley back to back. The hearthstone
  is bound there too. Bots are excluded automatically because the bot module
  creates characters through a different path.
- **Quests by faction (155e):** SQL widens any quest limited to some races of
  one faction to the whole faction. Original values are backed up, so the revert
  is exact.
- **Level-64 heroics (155f):** Ramparts, Blood Furnace, Slave Pens, Underbog and
  Mana-Tombs open heroic at 60, with heroic creatures at 64. Creature health and
  damage come from level times a per-creature multiplier, so no separate stat
  changes are needed.

## Tested so far
- **Config values:** `scripts/test-profile-config-gates` returns 20 of 20
  correct, and finds no patch editing a setting that doesn't exist.
- **Source patches:** `scripts/test-source-patches basic` shows every patch
  applies, reports success, and reverts to a byte-identical tree. Vanilla and
  release pass too.
- **Valley rotation code:** the patched file passes a full compiler type check
  using the build's own flags. A deliberately planted error was caught, so the
  check is real.
- **Not tested:** the three new SQL files haven't run against a database,
  because MySQL is down and you asked me not to start anything. After install,
  run `scripts/validate-basic-state` to check the database side.

## Bugs found and fixed
- **Install broke other profiles:** the first install of any new profile dropped
  and recreated the MySQL user `ritz`, granting it only the new profile's
  databases. Every other profile would have lost access to its data. It also
  created release and beta databases under names their config doesn't read, and
  never created the playerbots database.
- **Settings that never applied:** three config patches edited settings that
  don't exist, so they did nothing on any profile. **Two of the fixes change
  other profiles on their next install:**
  - run speed really becomes 80% everywhere (everyone has been running at 100%);
  - beta's instant flights really turn on.
- **Failures reported as success:** both patch runners said "Applied" and
  "complete" whatever happened. They now stop at the first failure and report
  what ran, what failed and what never ran.
- **Revert trap armed too late:** the compile script armed its revert-on-exit
  step only after applying patches, so a failure partway through would have left
  patches in the tree. It now arms first.
- **Database registration:** errors were thrown away and are now reported. When
  MySQL is down, the step still defers as designed but prints a notice.
- **Stale SQL:** edits to shared SQL source files never reached an existing
  install; they now do.
- **Accuracy patch revert (B005):** it left a stray blank line, so the source
  tree never returned to its original state.
- **Lua directory:** a missing profile Lua directory was silently created empty;
  it's now an error.
- **Display and docs:** the profile listing showed the source tree tracking
  upstream when it's pinned. Custom-class issues still used old issue numbers,
  one of which now collides with the new basic issue (155). The vanilla doc had
  the wrong starting towns and ports.

## Found but not fixed
- **Beta:** its patches B006 and B007 leave edits behind when reverted. It's
  harmless because every compile resets the source tree first.
- **Issue formatting:** 152 lacks two required sections, and 705 has no `.md`
  extension. Both predate today.
- **Custom classes:** the existing custom-class code is switched off
  (`src/lua-beta/custom-classes.lua.disabled`) and beta-only, so starting custom
  classes on basic means porting it, not flipping a switch.
- **Before committing:** my whole-file claim on `docs/table-of-contents.md` also
  covers another session's uncommitted `context-placement.md` line. That line
  has to be kept out of my commit.

## Open questions
1. What are vanilla and release called now? Is release meant to cap at 20 (what
   its config does) or 80 (what older notes said)?
2. Should basic's XP rate stay at 1×?
3. Do the two factions rotate independently (what I built), or on one shared
   counter?
4. Foreign valleys lack some class trainers (a human paladin landing in
   Shadowglen can't train). Should we spawn trainers there, use the universal
   class trainer issue (502), or something else?
5. Racial mount vendors: open them to the whole faction too, or keep them
   racial?
6. Outland items requiring level 61–62: leave them unusable at 60, lower them
   to 60, or make level-60 copies?
7. Heroics: is the list of five dungeons right, what should the loot be (stock
   heroic drops need level 70), do the reputation keys stay, and is heroic a
   second mode or a replacement for normal?
8. Custom classes: can players combine any two classes freely, or pick from
   curated classes? Your description and the recorded design differ; your words
   are now in 705.
9. Expert: do basic characters carry over at 60, or does expert start fresh
   (which would need a starting kit)?

If it's useful, I can test the three SQL files against a throwaway MySQL
instance in RAM, separate from the project database. I held off because you
asked me not to run anything.

--------------------------------------------------------------------------------

### User Request 5

<pasted_content id="51c5">
> - Rotating starting valleys (155d): a new source patch (B028) hooks character
>     creation. Each faction's current valley changes every 30 characters in a
>     shuffled order that never repeats a valley back to back. The hearthstone is
>     bound there too. Bots are excluded automatically because the bot module creates
>     characters through a different path.
</pasted_content id="51c5">


we should modify that path so that whenever a player character is created, a bot
is created as well, so players always have at least one buddy.

>     - beta's instant flights really turn on.

instant flights? I thought we disabled flights? Is there an issue file for that?



<pasted_content id="51c5">
> - Display and docs: the profile listing showed the source tree tracking
> upstream when
>     it's pinned. Custom-class issues still used old issue numbers, one of which now
>     collides with the new basic issue (155). The vanilla doc had the wrong starting towns
    and ports.
</pasted_content id="51c5">


tell me about the discrepancies?

> - Beta: its patches B006 and B007 leave edits behind when reverted. It's
> harmless because
>     every compile resets the source tree first.

not harmless. Points to a flaw in the code.

> - Issue formatting: 152 lacks two required sections, and 705 has no .md
> extension. Both
>     predate today.

housekeeping. Please do.

  2. Should basic's XP rate stay at 1×?

yes, 1x

> 3. Do the two factions rotate independently (what I built), or on one shared
> counter?

independently. Alliance and Horde will never spawn in the same valley, so
there's no reason to share a counter.



<pasted_content id="51c5">
> 4. Foreign valleys lack some class trainers (a human paladin landing in
> Shadowglen can't
>      train). Should we spawn trainers there, use the universal class trainer issue (502),
>      or something else?
</pasted_content id="51c5">


universal class trainer who only trains classes that aren't present in that
valley.

> 5. Racial mount vendors: open them to the whole faction too, or keep them
> racial?

they stay racial

> 6. Outland items requiring level 61–62: leave them unusable at 60, lower
> them to 60, or
>      make level-60 copies?

they stay at their required level. Can we modify this without patching the user
client or modifying mpqs? Will they notice, or do they need to clear their cache
or whatever?



<pasted_content id="51c5">
> 7. Heroics: is the list of five dungeons right, what should the loot be (stock
> heroic
     drops need level 70), do the reputation keys stay, and is heroic a second mode or a
     replacement for normal?
</pasted_content id="51c5">


the loot should be their default equipment lists.

> 8. Custom classes: can players combine any two classes freely, or pick from
> curated
     classes? Your description and the recorded design differ; your words are now in 705.

let's walk through the two designs and see how they differ.

> 9. Expert: do basic characters carry over at 60, or does expert start fresh
> (which would
>      need a starting kit)?

start fresh. But that's for a later development cycle. We'll need to copy the
starting equipment patch and issue and modify it for level 60 characters. I'll
need to hand-pick the equipment, of course... Can you create a tool which
outputs a picture of the character with the intended equipment equipped? Not
sure how we could do that, maybe a custom renderer? Is there an open source one
we could download? Anyway making a grid of pictures of the starting equipments
will help us pick them easier. This should also help with the level 20 sets as
well. We will use these pictures to assess them for aesthetic accordance. The
pictures should be auto-generated. But that's another issue file.

We'll also need the starting abilities for level 60, and maybe other patches as
well.

--------------------------------------------------------------------------------

### Assistant Response 5

> **[Asked the user]**
>
> *Q1 — Buddy kind:* Buddy bot: what is the buddy? The bot module already has
> a pool of pre-made bot characters it can lend to a player (its 'addclass'
> command levels one to your level and adds it to your group), but those are
> shared and handed back when you log out.
> - Dedicated new character (Recommended) — Creating your character also
>   creates a brand-new bot character that belongs to you. It starts in the same
>   valley at level 1, logs in when you do, joins your group, and levels
>   alongside you. It persists across sessions.
> - Borrowed from the pool — On each login, a pool bot of the chosen class is
>   levelled to you and added to your group. Simpler, but a different bot each
>   time.
> → **Answered:** Dedicated to your character, and every 10 levels another one
> joins your party. They don't join other people's party, and they do quests and
> stuff that are in the same regions as you. They instantly receive every quest
> that you do and complete it automatically whenever you do. They try and fight
> the same creatures in the same area that you do, but they might be on the
> other side of the patch of mobs and that's okay. You only share exp when
> you're close, and they don't try to stay near - they just attack mobs as if
> the NPC bot was fighting in that particular area on their own.
>
> *Q2 — Buddy class:* Buddy bot: which class is it?
> - Complements you (Recommended) — Fills the role you lack: a healer for a
>   damage or tank class, a tank for a healer. The race is picked at random from
>   your faction.
> - Random — Any class your faction can play, chosen at random.
> - Player chooses — Picked in-game on first login, through a gossip menu or a
>   chat command.
> → **Selected:** Player chooses
>
> *Q3 — Heroic keys:* Level-64 heroics: each one also needs a key bought at
> revered reputation (Honor Hold/Thrallmar, Cenarion Expedition, Lower City).
> Keep the keys?
> - Drop the keys on basic — Any level-60 party can enter heroic.
> - Keep the keys — Heroic stays a reputation reward, as in the stock game.
> → **Answered:** it's not actually a heroic dungeon. The regular dungeons are
> simply increased in difficulty. The monsters are set to level 64 and given
> appropriate damage / health values and such. The loot stays the same, so a
> level 58 can't get hardcore blues - they need to quest or farm mobs to get the
> greens and blues and epics that compare to the dungeon loot. They could of
> course also do vanilla content but Outland is an option. The dungeons are
> supposed to be as hard as raids or whatever.
>
> *Q4 — Heroic mode:* Level-64 heroics: is heroic a second difficulty next to
> the normal 60-65 version, or does it replace normal?
> - Second difficulty (as built) — Normal mode stays stock. Heroic is the
>   level-64 version with the normal-mode loot tables (your 'default equipment
>   lists' answer).
> - Replace normal — Normal mode itself moves up to level 64 and there is only
>   one version.
> → **Answered:** see answer to previous question.

> Starting with housekeeping.

> Housekeeping done: 152 now has both sections and 705 is renamed to `.md`. `git
> mv` recorded the rename in the shared staging area. Now the beta revert flaw
> (B006/B007).

> Beta now round-trips byte for byte. Next, a compile-level check that the
> rewritten patches still produce valid code. I'm turning the one-off syntax
> check from earlier into a reusable script.

> While that runs: 155f is now "normal dungeons at level 64, loot unchanged".
> Rewriting its SQL, issue and checks to match.

> While that runs: I can't tell from the data alone whether those "X Trainer"
> goblins are visible to players. Checking the upstream history for what they're
> for.

> Now the universal-trainer SQL. It uses the renamed column, and the native
> trainers whose look and faction each valley's trainer copies were chosen from
> the data above.

> Syntax checks are still running. Meanwhile, writing the buddy-bot issue.
> Checking which phase bot companions belong to.

> Writing the buddy issue as 617, in phase 6 (Companions). It carries your words
> verbatim, a split into parts, and the questions that block building it.

> Next, the issue for the equipment picture-grid tool. Getting its number.

> While the basic compile check finishes, updating 155c to record the B006/B007
> fix, the new compile-check tool, and the beta E012 finding.

> Basic's compile check passes on all 40 files its patches change. Last updates:
> the basic doc page, then the report.

Most of your list is done. The buddy bots and the portrait-grid tool are written
up as issues (617 and 159) but not built, because a few design questions block
them. I haven't committed, compiled or started anything.

## Your questions

**Instant flights on beta.** Only vanilla and basic remove flight paths; beta
never ran that step, so beta has always had normal flights. Its config patch
C009 was meant to make them instant for testing. It has no issue file of its
own; it's only mentioned in the completed config-orchestrator issue (119) and
the patch registry. It edited a setting that doesn't exist (`Instant.Taxi`; the
real one is `InstantFlightPaths`), so it never worked. With my fix, beta's
flights become instant on its next install. Do you want that, or should I take
C009 out?

**The discrepancies:**
- **Profile listing:** since July, install and update pin the shared source tree
  to commit `52f58186a533`. `./scripts/profiles` still showed release, beta and
  vanilla as tracking the latest upstream, because its own table was never
  updated.
- **Old issue numbers:** the custom-class issues still used numbers from before
  the renumbering. For example, 706 listed "155 (custom-class-selection-npc)",
  which is now 705, while 155 is now the basic profile. I fixed these in 705,
  706, 707, 709, 711 and 712 using the project's migration map. Issue 804 said
  "issue 156" for the accuracy cap (now 803, while 156 is now expert).
- **Vanilla doc page:** it said every Alliance character starts at Darkshire and
  every Horde character at Tarren Mill; since the starting-town work each race
  has its own town. It gave ports 4464/4364; the real shared ports are
  4462/4362.

**Changing items without touching the client.** Required level lives only on the
server, so changing it needs no client patch or MPQ edit, and the server
enforces the new value immediately. The client, though, keeps a cache of item
tooltips (`Cache/WDB`) and keeps showing the old value until that folder is
deleted. Display, item class and slot come from the client's own data files;
that's why cloned item IDs render invisible (148v). You've decided these items
keep their level, so nothing changes.

**The two custom-class designs.**

| | Recorded design (705/709/711) | Your description |
|---|---|---|
| Who composes the class | Designers: fixed classes like Spellblade and Knight | The player: any mix, "warrior mage, necromancer druid" |
| How it's chosen | A selector NPC at level 1 | By which trainers you visit |
| Spell list | A fixed list per class | Open |
| Talents | A curated tree per class (AIO) | Not said |
| Resource cost | Not addressed | Borrowed abilities re-costed in your resource, normalized by how often that kind of class casts that kind of ability |

Issue 502 (another session's in-progress work) already designs something close
to your resource conversion. Questions back to you:
- In your version, what stops a character from taking the best ability of every
  class? Is it a limit on how many, gold, or something else?
- What is the "middle-ground class" itself: a real base class that decides armor
  and resource, or something new?
- If a warrior borrows Fireball, what should it cost in rage so that it's used
  as often as a mage uses it, and how would you measure "as often"?

## Done this round
- **Housekeeping:** 152 has its missing sections, now reflecting the
  basic/expert decision. 705 is renamed to `.md`; `git mv` recorded the rename
  in the shared staging area. 75 other issue files also lack `.md`; I left them
  alone. Want those renamed too?
- **B006/B007 fixed:** the undo tried to re-match the inserted text instead of
  removing a marked block. B006 never removed the `#include` it added, and it
  left empty `#ifdef` shells. B007's pattern missed its first method because the
  method's comment wraps onto a second line. Both now mark every insertion with
  begin/end comments and delete exactly those. Beta now reverts cleanly.
- **155f redone as you described:** every creature spawned in the five dungeons
  is level 64, the loot is unchanged, and heroic is left stock. Health and
  damage scale with level automatically.
- **Visiting Mentor (155e):** one per valley, placed next to the spawn point,
  teaching only the faction classes that valley lacks. It uses only database
  changes: a trainer with no class restriction, whose training window already
  filters spells by the player's class. The classes to teach are worked out from
  the data, not hard-coded.
- **Answers recorded in the issues:** XP 1×, separate counters per faction,
  racial mounts stay racial, Outland items keep their required level.
- **Expert (156):** records "start fresh" and a table of vanilla's level-20
  head-start pieces to copy for level 60: kit, abilities, auto-equip, bot kit,
  professions, spawn point.
- **Portrait grid (159):** issue only. It lists three routes, the first most
  likely to be faithful:
  - photograph the real client with bots in a studio spot;
  - an open-source viewer (WoW Model Viewer is the likeliest; whether it still
    reads 3.3.5 data and can run from a script is unverified);
  - writing our own renderer.

## Found this round
- **Spawn-table column renamed upstream:** an upstream update renamed the spawn
  table's `id1` column to `id`. My new SQL had it wrong; now fixed. **Beta's
  creature-cleanup SQL (E012) still uses `id1` and would stop a fresh beta
  database's first boot.** I didn't change it: it deletes creatures, and editing
  the file makes the server re-run it on beta's existing database. Should I fix
  it?
- **Generic trainer NPCs:** NPCs named "Warrior Trainer", "Druid Trainer" and so
  on, with goblin models, stand in every valley, one of each class, wired to the
  real trainer lists. I can't tell from the data whether players can see them.
  Can you look in-game?
- **New compile check:** `scripts/test-patched-syntax` has the compiler check
  every file a profile's patches change, using the last build's flags, without
  building anything. All 40 of basic's changed files and all of beta's pass.

## Still untested
The four new basic SQL files have not run against a database. The column rename
shows why they should before you deploy. I can test them on a throwaway MySQL
instance in RAM, separate from the project database. Should I?

## Questions blocking the buddy bots (617)
1. A group holds five including you, and a buddy every 10 levels means a fifth
   buddy at level 40. Raid, a cap of four, or extras stay ungrouped?
2. Does a new buddy arrive at your level or at level 1?
3. Where do buddies live: on a hidden linked account, or on your own account
   using character slots?
4. How do you choose the class: an NPC menu, a chat command, or an addon window?
   And what happens if you never choose?
5. If a buddy dies, it presumably comes back like a player; does deleting your
   character delete its buddies?

Remaining questions for basic:
- Is the five-dungeon list right?
- Should creatures that scripts summon inside those dungeons also be level 64?
- Do vanilla and release keep their names, and does release's current level-20
  cap stay?

--------------------------------------------------------------------------------

### User Request 6

<pasted_content id="51c5">
> Instant flights on beta. Only vanilla and basic remove flight paths; beta
> never ran that step, so beta has always had normal flights. Its config patch
> C009 was meant to make them
> instant for testing. It has no issue file of its own; it's only mentioned in
> the completed config-orchestrator issue (119) and the patch registry. It
> edited a setting that doesn't
> exist (Instant.Taxi; the real one is InstantFlightPaths), so it never worked.
> With my fix, beta's flights become instant on its next install. Do you want
> that, or should I take C009
  out?
</pasted_content id="51c5">


we should leave it in, in the fixed form.



<pasted_content id="51c5">
> Changing items without touching the client. Required level lives only on the
> server, so changing it needs no client patch or MPQ edit, and the server
> enforces the new value
> immediately. The client, though, keeps a cache of item tooltips (Cache/WDB)
> and keeps showing the old value until that folder is deleted. Display, item
> class and slot come from the
> client's own data files; that's why cloned item IDs render invisible (148v).
> You've decided these items keep their level, so nothing changes.
</pasted_content id="51c5">


big, if true. Can you give me a list of similar information that is stored on
the server? I think I remember there was a way for the server to force a cache
reset on users connecting to the server for the first time, is that true?

  │ Who composes the │ Designers: fixed classes like Spellblade and │ The
  player: any mix, "warrior mage, necromancer druid" │
  │ class │ Knight │ │

yes they are curated.

  │ How it's chosen │ A selector NPC at level 1 │ By which trainers you
  visit │

both designs are true. Selector NPC picks the class, and the abilities are
learned from the respective class trainers. We don't have custom abilities, but
we might have for example a character with Rejuvenation and Heroic Strike.
They'd train Heroic Strike at the warrior trainer, and Rejuvenation at the druid
trainer. We'll also need the custom class trainer for each city that trains each
class that isn't present at that city / starter zone / wherever class trainers
are known.

  │ Spell list │ A fixed list per class │ Open │

yes it's a fixed list, and we choose which abilities they learn. I think their
required level has to be fixed at the same place it is normally, so we just
describe the abilities they want and which ranks we want them to be able to
train, and they'll learn them at the required levels.

  │ Talents │ A curated tree per class (AIO) │ Not said │

Ah... Yes... We should instead update this to have 3 talent trees, one for each
class, same as any other class. But those three talent trees might be different.
For example Rejuvenation means resto druid's talents, and Heroic Strike might
mean warrior's arms spec talent tree, and maybe there's another like survival
hunter or something. They should generally reflect the class shape that the
class turned out to be.

  │ Resource cost │ Not addressed │ Borrowed abilities re-costed in your
  resource, normalized by how often that kind of class casts that │
  │ │ │ kind of ability │

I wish we could adjust talents as well. Let's see how things go.

> - If a warrior borrows Fireball, what should it cost in rage so that it's used
> as often as a mage uses it, and how would you measure "as often"?

I'd probably say... 20 rage?

I mean, it's got a cast time, so we could charge the rage at the start of the
cast teehee

... that's evil, idk if we can manage that but we'll see.

Oh! And energy can drain it's cost from your energy bar as the cast time
increases!

diabolical

Focus is spent at the completion of the cast time.

> - Visiting Mentor (155e): one per valley, placed next to the spawn point,
> teaching only the faction classes that valley lacks. It uses only database
> changes: a trainer with no class
>     restriction, whose training window already filters spells by the player's class. The classes to teach are worked out from the data, not hard-coded.

they shouldn't have every ability to teach, I think the window breaks. They
should only have the spells for the classes that are missing trainers in that
area. We might have to gate it with a conversation dialogue option, that might
make things easier and a bit more abstracted and organized rather than just
throwing them all into one trainer window. "Hello can you teach me druid spells"
sure thing here they are

    - writing our own renderer.

this might be a useful issue to link doubly linked list style to the related
issue(s) in the /home/ritz/programming/ai-stuff/world-edit-to-execute/ project
directory, specifically phase W which is being architected as we speak. Check
back later to make sure any connections you write are still up to date.

> - Generic trainer NPCs: NPCs named "Warrior Trainer", "Druid Trainer" and so
> on, with goblin models, stand in every valley, one of each class, wired to the
> real trainer lists. I
>     can't tell from the data whether players can see them. Can you look in-game?

can you pick a powerful level 40ish trainer's model instead of the placeholder
goblin? for one of the missing classes and add it to that zone instead. So like
eversong woods might have a tauren druid model class trainer that teaches the
abilities of druid, and any other class that blood elves can't be like shaman.
But that way they feel a bit more thematic.

  1. A group holds five including you, and a buddy every 10 levels means a fifth
     buddy at level 40. Raid, a cap of four, or extras stay ungrouped?

we should dynamically adjust which ones are in a party as you based on which are
closest. Remember, they're just adventuring in the same zone as you. Same areas
too. If you enter a dungeon, then enough join to make a dungeon party. We can
choose them randomly, without replacement, cycling through to make sure everyone
gets a chance to go.

>   2. Does a new buddy arrive at your level or at level 1?
> 3. Where do buddies live: on a hidden linked account, or on your own account
> using character slots?

they arrive at your level.

they are on a hidden linked account. Playerbots.

> 4. How do you choose the class: an NPC menu, a chat command, or an addon
> window? And what happens if you never choose?

an NPC menu, there should be one at the starting zone, and one that spawns near
you and walks around, sits by a fire, tells jokes... But stays in the same area
as you were when you leveled up. If you log out, then when you log back in
they'll appear where you logged back in at. If there's more than two in an area
(maybe 30 yards or so) then they won't spawn. I'm thinking it should be a suave
human male with pale, almost ashen skin, black hair parted down the middle and
chin length, with a black and white shirt on (not tuxedo, more like inverted,
white shirt with black accents) and pale green pants. No cloak, helmet, weapon,
or shoulderpads, but we can pick belts and boots. No gloves probably either.
Though maybe he levels up with you idk not sure yet. We'll pick the exact outfit
later. See the immortal issue files for a reference implementation about how to
do the idle behavior, we want to be a little distinct from them but there might
be some good ideas you can use in there.

> 5. If a buddy dies, it presumably comes back like a player; does deleting your
> character delete its buddies?

yes it comes back to life. It'll move to the areas that the player is in when it
can. Also, deleting your character does delete it's buddies.

> - Should creatures that scripts summon inside those dungeons also be level 64?

yes

--------------------------------------------------------------------------------

### Assistant Response 6

> Starting with the cache question, checked against the server source rather
> than memory.

> Your memory is right: the server sends a cache version number at login, and
> the client wipes its cache when the number changes. Reading how it's
> configured.

> The fingerprint works: 4,142,694,974 for basic, which fits the setting's
> 32-bit range. Adding it to the test and writing issue 160.

> All nine anchors match exactly once in the current source. Adding B029 to
> basic's list, then running the round-trip and compiler checks.

> An off-by-one in the undo: it restored each replaced line with one leading
> character too many. Fixing and re-running.

> Round trip is now byte-identical. Starting the compiler check in the
> background and rewriting the mentor SQL for per-class dialogue.

> Adding the summon passes to the 155f SQL.

> Issue 155f wrongly says the validator lists every template raised to 64; I'll
> correct that. First, the "immortal issue files": nothing in this project
> matches, so searching your other projects.

> Writing the six sub-issue files. First, checking how the bot module creates
> and logs in characters, so 617a and 617c name real functions.

> B008 skips silently when its module folder is missing. Checking whether that's
> happening now.

> Now the reverse links in 159.

> Also correcting 159's summary of 148v to match what 148v actually says. Then
> checking the compile run.

Everything from your message is either built or written up as issues. Nothing is
committed, compiled or started. Offline checks pass: basic's source patches
apply and revert byte for byte, all 44 files they change pass the compiler's
type check, and the config test passes 21 of 21.

## The client cache
You remembered correctly. At login the server sends a "cache version" number,
and when it differs from the client's stored one, the client deletes its whole
`Cache/WDB` and asks again. The setting is `ClientCacheVersion` in
`worldserver.conf`; it has been 0 on every profile, meaning "use the upstream
database's number", which none of our edits change. All profiles also share one
client cache, so switching realms carries stale data across.

I built config patch C025 (issue 160). It sets that number to a fingerprint of
the profile name, the source commit and the profile's SQL edits. It changes
exactly when our content does and differs per profile, so the client refreshes
itself.

What lives where:

| Where | What | How a change reaches players |
|---|---|---|
| Server only | Loot, drop rates, vendor and trainer lists and prices, dialogue options, creature level/health/damage, spawns and AI, quest logic, rules enforced on equip (required level, class/race), rates | Immediately |
| Server, cached by the client | Item tooltips (name, stats shown, required level, price), creature name/title/elite frame, object names, quest text and rewards, NPC dialogue text, book pages | C025 refreshes the cache |
| Client files only (needs an MPQ patch) | Spells (cost, power type, cast time, range, tooltip), talent trees, item appearance and slot, character-creation race/class combos and preview outfits, maps, zones, flight map, factions | Needs a client patch |

Two consequences:
- **Resource conversion:** the client checks a spell's cost and power type from
  its own spell data before asking the server. A warrior casting stock Fireball
  is refused by the client for lack of mana, so any re-costing needs a client
  patch or new spell entries. That's now written into 710.
- **Cloned items (148v):** a new item ID gets its look only from the cache, and
  a cache reset doesn't help with that.

## Done this round
- **C009:** kept in its fixed form, so beta's flights are instant from its next
  install.
- **Custom classes:** recorded your answers verbatim in 705: curated classes,
  abilities learned from each source class's own trainer, stock ranks and
  levels, and three talent trees taken from the source specs (also noted in
  713). Your resource brainstorm is in 710 as a table: rage charged at cast
  start, energy drained over the cast, focus paid at completion. One engine fact
  also recorded: a trainer's per-spell class check would refuse a warrior-based
  class Rejuvenation even at a druid trainer, so custom-class training needs a
  source patch.
- **Visiting Mentor, reworked:**
  - **Dialogue:** one line per missing class ("Can you teach me the ways of the
    druid?"), and each line opens only that class's list. The server already
    hides lines for other classes, so a player sees only their own.
  - **Source patch B029:** makes this possible the same way stock vendors choose
    their shop list. Stock trainers are untouched.
  - **Looks:** each mentor copies a level 50–70 trainer of a missing class
    from another race, for example Sheal Runetotem (tauren druid) on Sunstrider
    Isle.
  - **Goblins:** the goblin placeholders near each valley are removed and saved
    so the revert can put them back.
- **155f:** creatures summoned by the dungeons' scripts go to 64 as well. The
  sources are the dungeons' C++ scripts, the database-scripted summons (followed
  two levels deep) and the summon groups. The Midsummer Ahune encounter is
  excluded on purpose, since it's holiday content with its own level.
- **Buddy bots (617):** your answers are recorded verbatim and split into six
  sub-issues: roster and creation, the selector NPC, login and proximity party,
  quest mirroring, adventuring in your area, and the selector's appearance. Each
  names the real bot-module functions to use. Your "immortal issue files" match
  the immortal shepherds in 911: they reappear at your logout spot and wander
  like the travellers but rarer. That's cited as prior art.
- **159 ↔ phase W:** linked both ways with W01 (client-file readers), W03
  (model rendering) and W04. W04 already designs route 1: real client under
  Wine, scenes set up by GM command, recorded unattended. The phase W side is
  being designed now, so the links will need re-checking.

## Found
- **Beta talent-bonus module missing:** beta's patch B008 links a module from
  `modules/mod-talent-bonus`, which doesn't exist, so every beta build quietly
  lacks issue 120's talent bonus. Should B008 fail loudly instead?
- **Beta E012, still waiting on you:** the creature-cleanup SQL still uses the
  old `id1` column, which would stop a fresh beta database's first boot. Editing
  it makes the server re-run it against beta's existing database.
- **Other issue numbers:** issue 804 still used 156 for the accuracy cap (now
  803; 156 is the expert profile), so I fixed it. B005's source comments keep
  "(Issue 156)" because its revert matches on that text.

## Still untested
None of basic's SQL has run against a database, and the renamed column shows why
it should. It would also confirm the mentor dialogue. Can I test it on a
throwaway MySQL in RAM, separate from the project database?

## Open questions (most useful first)
1. Should Visiting Mentors also go to every other place with trainers, meaning
   all the goblin-placeholder spots? Your 705 answer suggests yes for custom
   classes.
2. Buddy names: random, or does the player name them? Race: random within the
   faction, the owner's race, or chosen?
3. Selector NPC: his name, and where the jokes come from (a written list or
   generated)?
4. Can the selector's look use a client patch? Your exact skin, hair and outfit
   needs a new display entry; reusing an existing NPC's display limits the
   choice.
5. Buddy quest rewards: a reward item chosen by its gear logic, or only XP and
   money?
6. Is the five-dungeon list for 155f right?
7. Do vanilla and release keep their names, and does release stay capped at 20?

--------------------------------------------------------------------------------

### User Request 7

Oh! We get to edit the vendor lists! that's so cool! I can't wait!

> - Resource conversion: the client checks a spell's cost and power type from
> its own spell data before asking the server. A warrior casting stock Fireball
> is refused by the client
    for lack of mana, so any re-costing needs a client patch or new spell entries. That's now written into 710.

ah dang... we might not be able to make cross-resource custom classes for now,
which is a real big downer. Maybe we can get around it with items that when used
will do something like, for example, put you in bear or cat form maybe, but with
your own model and able to cast warrior and rogue abilities instead of bear and
cat abilities? would that be doable without a cache reset?



<pasted_content id="51c5">
> - Custom classes: recorded your answers verbatim in 705: curated classes,
> abilities learned from each source class's own trainer, stock ranks and
> levels, and three talent trees
    taken from the source specs (also noted in 713). Your resource brainstorm is in 710 as a table: rage charged at cast start, energy drained over the cast, focus paid at completion.
>     One engine fact also recorded: a trainer's per-spell class check would refuse a warrior-based class Rejuvenation even at a druid trainer, so custom-class training needs a source
    patch.
</pasted_content id="51c5">


that can't be true! It'll harm most of our plans if true. Let's re-evaluate.

> - Beta talent-bonus module missing: beta's patch B008 links a module from
> modules/mod-talent-bonus, which doesn't exist, so every beta build quietly
> lacks issue 120's talent bonus.
>     Should B008 fail loudly instead?

yes it should fail loudly.

> - Beta E012, still waiting on you: the creature-cleanup SQL still uses the old
> id1 column, which would stop a fresh beta database's first boot. Editing it
> makes the server re-run it
>     against beta's existing database.

we should fix that.

> 1. Should Visiting Mentors also go to every other place with trainers, meaning
> all the goblin-placeholder spots? Your 705 answer suggests yes for custom
> classes.

yes. No duplicates!

> 2. Buddy names: random, or does the player name them? Race: random within the
> faction, the owner's race, or chosen?

random name, random race. Compatible with the class that the character picked,
of course.

> 3. Selector NPC: his name, and where the jokes come from (a written list or
> generated)?

sargobras, and his jokes should come from a written list, but we have to add to
it every once in a while.

> 4. Can the selector's look use a client patch? Your exact skin, hair and
> outfit needs a new display entry; reusing an existing NPC's display limits the
> choice.

no client patches allowed until the custom client is in.

> 5. Buddy quest rewards: a reward item chosen by its gear logic, or only XP and
> money?

what would the playerbots pick? they level up according to the same rules that
the player does. How do they pick gear rewards?

>   6. Is the five-dungeon list for 155f right?

anything in Outland that can be reached by a level 60 should be scaled to level
64.

> 7. Do vanilla and release keep their names, and does release stay capped at
> 20?

I dunno we're not really working on vanilla and release right now so let's just
leave them alone.

--------------------------------------------------------------------------------

### Assistant Response 7

> Re-checking the trainer claim in the source first.

> Rage gain is gated by a "has active power type" check. Reading it.

> Checking every other SQL source in the project for the same renamed and
> dropped columns.

> Now B008: failing loudly when its module is missing.

> The module did exist in git; commit `e42b59e` (profile-transition preparation)
> touched it last. Finding where it went.

> Faction groups read cleanly: 2 is Alliance, 4 is Horde. Writing the generator.

> Restructuring the mentor SQL around the generated site table.

> Rewriting the 155f SQL for all reachable Outland, and renaming its files to
> match (they're new and not yet in git).

> Updating the revert to drop the working tables too, and the validator's 155f
> checks.

> Rewriting issue 155f for the wider scope and fixing the remaining references.

> 155f is done. Now the bot module: how playerbots choose quest reward items.

> Closing the naming question in 152 and 155 as "left alone", then a final round
> of checks.

Your cross-resource plan is much more alive than I said. Everything else from
your message is done or written into its issue. Nothing is committed, compiled
or started; the offline checks pass (config 21 of 21, basic's source patches
revert byte for byte).

**Vendors:** yes, vendor lists (`npc_vendor`) live only on the server and change
immediately; the client doesn't cache them.

## Re-evaluated: custom classes and resources
I overstated both problems.

**Training.** There are two server-side gates, and neither is hard:
- **Whole trainer ("class trainers teach only their class"):** the Visiting
  Mentor mechanism already gets around this. A dialogue line can open any
  class's list, and it can be offered only to a given custom class.
- **Each spell ("can this class learn it"):** the server reads its own copy of
  the client's skill tables, and a database table can override them
  (`skilllineability_dbc`). So allowing a class a spell is a data edit. That
  applies to the whole class, though; limiting it to the custom-class character
  alone takes a few lines of source patch.

**Resources.** In the server source:
- **Pools:** every character already has a rage pool and an energy pool whatever
  their class, and the server regenerates energy and mana for every player each
  tick.
- **Hooks:** AzerothCore ships two script hooks for exactly this. One turns on
  rage gain for any class; the other can give a warrior a mana pool.
- **So:** the server side is a small module, not a core patch, and your
  item-and-form idea isn't needed.
- **Not possible yet:** re-costing (rage charged at cast start, energy drained
  over the cast, focus spent at completion) changes a spell's cost in the
  client's own data, so it waits for the custom client.

One unknown is left, and two GM tests on a warrior settle it:
1. `.modify mana 1000`, then `.learn 133` (Fireball), then cast it. Does the
   client let a warrior with a mana pool cast a mana spell?
2. `.learn 774` (Rejuvenation). Does it appear in the spellbook?

It's all written into 705 and 710.

## Done
- **B008 now stops loudly:** that means **beta's compile will stop at B008**.
  The talent-bonus module was retired on purpose: disabled in `e42b59e`, deleted
  in `afc93e5`. Should I take B008 off beta's list?
- **Beta E012:** changed `id1` to `id`. Its setup step only copies the file when
  the output file lacks its marker, so beta's existing database won't re-run it;
  fresh installs get the fix.
- **Another break, caused partly by me:** vanilla's riding-gate SQL (shared by
  basic) still updated the `npc_trainer` table, which upstream dropped in
  December 2025. My earlier change makes that step recopy its SQL when the
  source changes, so vanilla's next install would have re-run it and failed. The
  dead lines are removed.
- **Mentors everywhere, never two:** the goblin placeholder stacks mark 24
  training places, three per race: valley, first town, capital. A new generator
  script (`scripts/generate-basic-mentor-hubs`) checks which real trainers stand
  around each place and places exactly one mentor where the goblins stood.
  Stormwind trains every class, so there are 23. Every look is unique, and your
  eight valley picks are kept.
- **All of reachable Outland at 64:**
  - **Scope:** the open world, plus the nine dungeons whose normal mode admits
    level 60. Raids and later dungeons stay out.
  - **What counts:** creatures that scripts summon count too.
  - **Safeguard:** anything also spawned in Azeroth is left alone, so no
    level-64 Elwynn rabbits.
- **Buddies:**
  - **Name and race:** random name; random race that can play the chosen class.
  - **Sargobras:** he's named, with a starter list of 12 jokes in
    `src/lua-basic/data/sargobras-jokes.lua` to grow over time.
  - **Look:** he reuses an existing display until the custom client exists.
  - **Quest rewards:** buddies choose like every bot. Upgrades they can equip
    come first, then gear they can wear, then usable items, with ties broken by
    their spec's stat weights. This needs `AutoPickReward = yes`, which is the
    default.
- **Vanilla and release:** left alone, and recorded as parked.

## Still untested
The four new basic SQL files have not run against a database. Today alone turned
up three schema changes (the renamed column, the dropped table, and MySQL's rule
against reusing a temporary table within one statement). Can I test them on a
throwaway MySQL in RAM, separate from the project database?

## Open questions
1. Take B008 off beta's list, since the module is retired?
2. Outland quests keep their stock levels (58–70) while creatures are now 64.
   Should quest levels follow? That affects experience and quest-log colours.
3. Is Sargobras's stationary valley NPC the only selector at level 1, or does
   the wandering one also appear then?
4. Does Sargobras's outfit level up with you? You were undecided.
5. Buddy group swapping: a starting interval and margin, e.g. every 5 seconds
   with a 10-yard margin, to tune later?
6. What level band of creatures should buddies fight, and should they roam the
   zone when your area is a town?

--------------------------------------------------------------------------------

### User Request 8

● Your cross-resource plan is much more alive than I said. Everything else
from your message is done or written into its issue. Nothing is committed,
compiled or started; the offline
>   checks pass (config 21 of 21, basic's source patches revert byte for byte).

can you start making git commits as we go?

> - Each spell ("can this class learn it"): the server reads its own copy of the
> client's skill tables, and a database table can override them
> (skilllineability_dbc). So allowing a
>     class a spell is a data edit. That applies to the whole class, though; limiting it to the custom-class character alone takes a few lines of source patch.

server source-patch is fine. But client patch is impossible. We should also not
require the users to reset their cache. I never heard back from you - is it
possible for the server to request a cache reset?



<pasted_content id="51c5">
>   Resources. In the server source:
> - Pools: every character already has a rage pool and an energy pool whatever
> their class, and the server regenerates energy and mana for every player each
> tick.
> - Hooks: AzerothCore ships two script hooks for exactly this. One turns on
> rage gain for any class; the other can give a warrior a mana pool.
</pasted_content id="51c5">


yo, what? big if true. No client patch required?



<pasted_content id="51c5">
>   One unknown is left, and two GM tests on a warrior settle it:
> 1. .modify mana 1000, then .learn 133 (Fireball), then cast it. Does the
> client let a warrior with a mana pool cast a mana spell?
>   2. .learn 774 (Rejuvenation). Does it appear in the spellbook?
</pasted_content id="51c5">


these are both required. Let's hope they work!

  - All of reachable Outland at 64:
    - Scope: the open world, plus the nine dungeons whose normal mode admits level 60. Raids and later dungeons stay out.

oh, um, no, the open world remains at the default levels. Just the dungeons are
modified.

> The four new basic SQL files have not run against a database. Today alone
> turned up three schema changes (the renamed column, the dropped table, and
> MySQL's rule against reusing a
> temporary table within one statement). Can I test them on a throwaway MySQL in
> RAM, separate from the project database?

sure if it stays in ram...

>   1. Take B008 off beta's list, since the module is retired?

don't worry about beta right now. Park it like beta and release.

  2. Outland quests keep their stock levels (58–70) while creatures are
     now 64. Should quest levels follow? That affects experience and quest-log
     colours.

quests and creatures should be the default level. Only dungeons are affected.

> 3. Is Sargobras's stationary valley NPC the only selector at level 1, or does
> the wandering one also appear then?

yes that's the only selector at level 1. The wandering one which stays near a
player UNTIL THEY CHOOSE, then he just hangs out near a nearby fire (that he
spawns) until no players are within sight range, then he despawns. His task is
to let you choose a new companion - once you've done so, then he waits until
nobody's looking, then turns into a black dragon and flies away. We can just
despawn him though, it happens offscreen.

>   4. Does Sargobras's outfit level up with you? You were undecided.

I am still undecided.

> 5. Buddy group swapping: a starting interval and margin, e.g. every 5 seconds
> with a 10-yard margin, to tune later?

yeah sure. The grouped buddies should try to move in the same area (we can
choose areas based on the area text that displays when you walk into for example
a town or a place) in the same general area that is A. fully pathable to your
location without leaving the radius, and B. the radius is the radius that
delivers exp. The ones outside of your group try to fight monsters and stuff in
the area outside of that radius. If they're busy fighting a monster and you move
away for example, then you might get put into a group with someone else, and
they'll get put into the group of characters that tries to move around in the
same area but outside of your exp radius, since they're not in your group. This
only matters at level 50+ I think, when you have 5 companions and only 4 can fit
in your party. Right?

> 6. What level band of creatures should buddies fight, and should they roam the
> zone when your area is a town?

they should stay in the same area as you. So if you're in town, they should
wander around town and stand in front of random NPCs, do some talking
animations, then walk to another NPC. Walk, not run. A random NPC in the area.
When you're in a town or a city, you should be un-grouped with them too.

--------------------------------------------------------------------------------

### Assistant Response 8

> Starting the commits. First, a preview of what the commit tool would take.

> The file name still says "reachable Outland", which now reads as the open
> world. Renaming to "outland dungeons", then fixing the validator, the issue
> and the docs.

> Committing in three pieces, first the fixes to shared machinery.

> Now the basic profile itself.

> The four SQL symlinks weren't in that commit. Checking why.

> Re-running basic's shared setup steps in a scratch copy to confirm they now
> read vanilla's files.

> Working. Committing that change, then the issue files and docs, leaving out
> the table of contents.

> The warning concerns my own earlier `git mv`, which left a staged entry for
> 705 in the shared staging area. It's mine, so I'm clearing just those two
> paths.

> Clean apart from other sessions' work; another session is writing a
> three-machine deployment plan (issue 157). Now the phase W back-links in the
> other repository.

> While that loads (a few minutes), recording your answers in the buddy issues.
> Checking the server's group-experience range, since your party rule is built
> on it.

> Waiting for the RAM database test to finish loading and running.

> Committing the test and these updates.

I've started committing as we go; there are six commits so far on `main`. All of
basic's SQL has now been tested against a real, fully updated database schema in
RAM, and every step and every check passed.

**Can the server force a cache reset?** Yes. I answered this last round, but it
got lost in a long message. At login the server sends a cache version number;
when it differs from the one the client stored, the client deletes its whole
cache and asks again. Players do nothing. The config patch C025 (issue 160) is
committed: it sets that number to a fingerprint of each profile's database
edits, so it changes exactly when our content does. What I haven't seen yet is
it working in the real client; that's step 3 in issue 160.

**"No client patch required?"** For the server side, yes; I read it in the
source. Every character already has rage and energy pools whatever their class.
AzerothCore has one script hook that turns on rage gain for any class and
another that can give a warrior a mana pool, so that's a small server module.
The client side is the part we haven't seen. It decides whether a warrior may
cast Fireball and whether Rejuvenation shows up in the spellbook, and the two GM
tests you marked required settle both. If the client refuses, the plan hits the
no-client-patch wall.

## Committed
1. **Build machinery fixes:**
   - install no longer drops the database user;
   - the patch runners stop and report instead of pretending;
   - three config patches that never took effect are fixed;
   - beta's patches now undo themselves cleanly;
   - two schema-drift breaks are fixed (the renamed column, the dropped
     `npc_trainer` table);
   - the new offline test tools.
2. **The basic profile itself.**
3. **Vanilla SQL shared by table:** basic now reads vanilla's three shared SQL
   files through a small table in the setup code. The commit tool can't carry
   symlinks, and the table is more visible anyway.
4. **Issues and docs.**
5. **The RAM database test** (`scripts/test-basic-sql-in-ram`), 155f narrowed to
   dungeons, and your latest buddy answers.

**Not committed:** my one line in `docs/table-of-contents.md` (the entry for
`basic.md`). That file also holds another session's uncommitted line, and my
commit would sweep it in. It can go in after they commit theirs.

The phase W back-links were already committed, because the session working on
phase W committed those files.

## Changed per your answers
- **155f:** only the nine Outland dungeons a level 60 can enter go to 64, which
  is 220 creature types, bosses included. The open world and quests stay stock.
- **Beta:** parked like vanilla and release. Its compile will still stop at B008
  (the retired talent-bonus module) until someone picks beta up.
- **617b, Sargobras:** the stationary selector is the only one at level 1. From
  level 10 he stays near you until you choose. Then he waits by a campfire he
  spawns until nobody is in sight, and despawns.
- **617c, the party:**
  - swaps checked every 5 seconds with a 10-yard margin;
  - grouped buddies stay within the group-XP range (74 yards by default) and
    ungrouped ones outside it;
  - areas are the on-screen place names;
  - everyone ungroups in town.
- **617e, towns:** buddies walk, never run, from one random NPC to another and
  play talking animations.

## Questions
1. **Buddy count:** one buddy at creation plus one each at 10, 20, 30 and 40
   means five buddies at level 40, so the extra one starts at 40, not 50. Is
   that the intended count, or should the first "every ten levels" buddy arrive
   at 20?
2. **Sargobras's "sight range":** the server's normal visibility distance, about
   100 yards outdoors?
3. **What counts as a town?** Should it be "you're in a rested area" (inns and
   cities), which the game already tracks?

--------------------------------------------------------------------------------

### User Request 9

<pasted_content id="51c5">
> Can the server force a cache reset? Yes. I answered this last round, but it
> got lost in a long message. At login the server sends a cache version number;
> when it differs from the
> one the client stored, the client deletes its whole cache and asks again.
> Players do nothing. The config patch C025 (issue 160) is committed: it sets
> that number to a fingerprint of
> each profile's database edits, so it changes exactly when our content does.
> What I haven't seen yet is it working in the real client; that's step 3 in
> issue 160.
</pasted_content id="51c5">


great if this is expected to be reliable, then we can open up our design
decision space to all things that require a cache reset as well. With the caveat
that they can't be changed dynamically at runtime. That's alright. Can you
describe the list again of the two categories we can do? Nothing that requires a
client edit should be considered.



<pasted_content id="51c5">
> "No client patch required?" For the server side, yes; I read it in the source.
> Every character already has rage and energy pools whatever their class.
> AzerothCore has one script
> hook that turns on rage gain for any class and another that can give a warrior
> a mana pool, so that's a small server module. The client side is the part we
> haven't seen. It decides
> whether a warrior may cast Fireball and whether Rejuvenation shows up in the
> spellbook, and the two GM tests you marked required settle both. If the client
> refuses, the plan hits
>   the no-client-patch wall.
</pasted_content id="51c5">


why don't we make it so that for example, a spellblade with warrior and mage
abilities, when talking to a warrior and a mage trainer, opens up not the
warrior and mage class training lists, but the spellblade training lists, with
the warrior spellblade spells on the warrior trainer, and the mage spellblade
spells on the mage's training list? That way, we can make duplicates that have a
different class requirement, which I think are available now without a client
edit. Confirm? Alternatively, make it so that any class can learn any spell,
remove the requirements completely, and populate specific class lists for the
custom classes (and the classes that need trainers in the new zones!) that just
have the intended spells.

> 3. Vanilla SQL shared by table: basic now reads vanilla's three shared SQL
> files through a small table in the setup code. The commit tool can't carry
> symlinks, and the table is more
>      visible anyway.

characters should be separate between servers, but world databases are okay to
share.

> - 155f: only the nine Outland dungeons a level 60 can enter go to 64, which is
> 220 creature types, bosses included. The open world and quests stay stock.

if any of the gear dropped requires higher than level 60, we should set it's
required level to level 60. This includes greens that might drop in those
dungeons.

can we also make a patch to disable the talent picks above 30 talents required?
For example, for fire mage, that's combustion. For ret paladin, it's repentance.
This should include the talents to the left and right of the "capstone" -
essentially, we're setting the max level to 60, and capping talents as they
would be in vanilla. This should be applied to basic.

  - 617b, Sargobras: the stationary selector is the only one at level 1. From
    level 10 he stays near you until you choose. Then he waits by a campfire he
    spawns until nobody is in
    sight, and despawns.

when he's waiting around the campfire, that's when he should tell jokes. He
should be "on break" so he should seem relaxed and aloof.

my mysqld is running pretty hot, can you tell me why? is the server started in
the background? don't forget to close it when you're not using it.

  1. Buddy count: one buddy at creation plus one each at 10, 20, 30 and 40 means
     five buddies at level 40, so the extra one starts at 40, not 50. Is that
     the intended count, or should
>      the first "every ten levels" buddy arrive at 20?

great even better

> 2. Sargobras's "sight range": the server's normal visibility distance, about
> 100 yards outdoors?

yep

> 3. What counts as a town? Should it be "you're in a rested area" (inns and
> cities), which the game already tracks?

nope, it should be towns and cities.

> - 617e, towns: buddies walk, never run, from one random NPC to another and
> play talking animations.

they should also sell items by prioritizing merchants if their packs are full (a
percentage fullness is a percentage priority). Also if they have new spells they
could learn, they should prioritize talking to a trainer.

--------------------------------------------------------------------------------

### Assistant Response 9

> Checking the hot mysqld first.

> The one running hot is mine: the RAM test server (PID 29136, 97% CPU). I
> deleted its data folder while it was still shutting down, and it has been
> spinning since. Stopping it now.

> Committing that fix. Then three code questions behind your ideas: whether the
> server strips spells a class "shouldn't" have, how talent learning can be
> refused, and how the game marks towns.

> Only the trainer code calls that class check, so nothing strips a learned
> spell elsewhere. Checking the spell-adding code for any other skill-based
> removal at login.

> The server has an empty "Custom" script folder with a loader, meant for
> exactly this. Writing the issue first (155g), then a source patch that copies
> a project-owned rules file into it.

> `IsBot()` exists in this tree, so the message goes to real players only. Now
> patch B030, bracketed like B029.

> The editor's errors are expected: it doesn't know the server's include paths
> for a file outside the source tree. The real check is the compiler run after
> B030 copies it in.

> Running the basic compile check in the background (about 10 minutes).
> Meanwhile, the dungeon loot rule: gear that drops in those nine dungeons and
> requires more than level 60 gets required level 60.

> Order is right: the loot step runs while its working tables still exist.
> Starting the RAM database test again (this time it shuts itself down cleanly)
> while I update the issues.

> Recording your custom-class training idea and my answer in 705, and your
> database-sharing rule in 155a.

> Adding 155g to the parent issue while the two checks run.

> Both pass: all 47 patched and added files compile-check clean, and all 17
> database checks pass. Confirming the new rules file was actually among those
> checked, and that the RAM server is gone.

> No RAM server left; the only match was my own search command. Committing.

The hot mysqld was mine, and it's stopped. It was the RAM test server: my
cleanup deleted its data folder before the process had exited, and it spun at
full CPU. The test now waits for the process itself to exit, and I've checked
that no copy of it is left running. The other two MySQL servers aren't mine:
your project MySQL and a system-wide one.

## What we can change without touching the client
These two categories hold if the cache reset works as expected. The server sends
the cache version, and AzerothCore's own documentation for the setting says it
triggers a client cache reset. What hasn't been seen yet is it happening in the
real client; that's step 3 in issue 160.

**1. Takes effect immediately, nothing cached:**
- loot tables and drop chances
- vendor lists and prices
- trainer lists and costs
- NPC dialogue options (the lines you click, and what each one does)
- creature levels, health, damage, faction and flags
- spawns, creature movement, AI and scripts
- quest logic (requirements, credit, rewards given)
- item rules checked when used or equipped: required level, which classes and
  races can use it, the stats you actually get
- XP and reputation rates, and everything in the server config
- the server's own copy of the skill and spell tables, which decides who may
  learn what
- all server code, including hooks like the talent cap

**2. Server data the client caches, refreshed by the cache reset:**
- item tooltips: name, quality, stats shown, required level, price, description
- creature name, subtitle, type, elite frame and portrait model
- object names and data
- quest titles, text, objectives and listed rewards
- NPC dialogue text, book pages, item text

Things that are not possible: new spells or changed spell costs, talent tree
layout, how items look, new race/class combinations at character creation, maps
and zones.

## Custom-class training
- **Duplicate trainer lists: confirmed.** A "spellblade (warrior side)" list on
  warrior trainers and a "spellblade (mage side)" list on mage trainers are just
  server data. Each trainer gets one extra dialogue line that opens it, using
  the same mechanism as the Visiting Mentors. A condition shows that line only
  to spellblades; the simplest marker is a hidden quest completed at the class
  selector.
- **Duplicate spells: not possible.** A copy of a spell with its own class
  requirement would be a new spell ID, and spells live in the client's files.
  Duplicating the lists gives the same result without that.
- **The per-spell class check** still has to be passed, with one of two
  server-side options:
  - **(A) Recommended:** a few lines of patch so lists without a class
    requirement (our curated lists) skip the class check and keep the race
    check.
  - **(B) Your alternative:** clear every spell's class restriction through a
    generated override table (`skilllineability_dbc`). It works, but it rewrites
    a world-wide table.

Both still depend on the two GM tests. It's written up in 705.

## Done and committed
- **Talent cap (155g):** Combustion and Repentance are both in tier 6, the row
  needing 30 points. Basic now refuses tier 6 and deeper, and tells the player
  why. It lives in a project-owned rules file compiled into the server's stock
  "Custom" scripts folder.
- **Dungeon gear:** in those nine dungeons, gear that required more than level
  60 now requires 60, greens included. That covers creature loot, chests and
  nested loot tables. Required level belongs to the item, so the same item
  dropping elsewhere changes too; on basic that only helps.
- **Tests:** all 47 files basic's patches add or change pass the compiler check,
  and the RAM database test passes all 17 checks. Both clean up after
  themselves.
- **Issues updated with your answers:**
  - Sargobras tells his jokes on break by the fire, relaxed and aloof.
  - Five buddies from level 40; sight range is about 100 yards.
  - "Town" means towns and cities.
  - Buddies in town sell at merchants with priority equal to how full their bags
    are, and go to their trainer first when they have spells to learn.
  - The database-sharing rule.

**Database sharing:** characters are already separate for every profile. Basic
can't share vanilla's world database, because the two profiles edit the world
differently; they share SQL source files, not databases. Your rule mostly
matters for the multi-machine deployment another session is planning (issue
157).

**Your one table-of-contents line** is still uncommitted because of the other
session's line in the same file.

## Questions
1. **Talent cut:** vanilla's trees let you take the tier-6 talent (its 31-point
   capstone). Your examples put tier 6 out, so that's what I built. Is that
   right?
2. **Bots at 60** keep the points they can't spend in capped rows. Should their
   talent plans be taught to spend them in the rows that are allowed?

--------------------------------------------------------------------------------

### User Request 10

> 2. Server data the client caches, refreshed by the cache reset:
> - item tooltips: name, quality, stats shown, required level, price,
> description

so, we can modify items, as long as there's a server reset between
modifications? excellent

> Things that are not possible: new spells or changed spell costs, talent tree
> layout, how items look, new race/class combinations at character creation,
> maps and zones.

we can't change the model ID for example for an item? Also, can we duplicate a
spell with a new ID? And change that one's spell cost?

> - Duplicate spells: not possible. A copy of a spell with its own class
> requirement would be a new spell ID, and spells live in the client's files.
> Duplicating the lists gives the
>     same result without that.

nuts.



<pasted_content id="51c5">
> - The per-spell class check still has to be passed, with one of two
> server-side options:
>     - (A) Recommended: a few lines of patch so lists without a class requirement (our curated lists) skip the class check and keep the race check.
>     - (B) Your alternative: clear every spell's class restriction through a generated override table (skilllineability_dbc). It works, but it rewrites a world-wide table.
</pasted_content id="51c5">


if we just ensure that only specific classes can open the specific trainer
windows, then the alternative probably is the best option I think.

>   1. Takes effect immediately, nothing cached:
> - the server's own copy of the skill and spell tables, which decides who may
> learn what

excellent

> - Talent cap (155g): Combustion and Repentance are both in tier 6, the row
> needing 30 points. Basic now refuses tier 6 and deeper, and tells the player
> why. It lives in a
>     project-owned rules file compiled into the server's stock "Custom" scripts folder.

okay so tier 6 should have only the capstone ability available. Is there a
reason this isn't applied with the patching system?

> - Dungeon gear: in those nine dungeons, gear that required more than level 60
> now requires 60, greens included. That covers creature loot, chests and nested
> loot tables. Required
>     level belongs to the item, so the same item dropping elsewhere changes too; on basic that only helps.

hmmmmmmmmmmmmmmm I think we should change it so that creatures in these dungeons
have +4 levels, with a max of level 64. So HFR for example might be doable at
level 59 or 58 even maybe, because it'd have mostly level 62 enemies. Jeez nine
dungeons... That's probably like, level 65 gear or above, right? That's too
powerful for a level 60 to have, though the difficulty to acquire it might make
it worth it.

> 2. Bots at 60 keep the points they can't spend in capped rows. Should their
> talent plans be taught to spend them in the rows that are allowed?

Yeah. Usually this is another spec tho - like 2/3rds arms, 1/3rd fury. Or 2/3rds
disc, 1/3rd shadow. Buddies never change their spec. There should also be a
1/3rd in each talent tree option. So we'll need to create "talent plans" for all
of these permutations, and estimate which talents to pick based on theme
analysis and also a double-pass scoring system that repeats continuously,
increasing the score for talents that aren't picked as much, searching for an
equilibrium where all talents are near equally chosen in all the permutations.
The main abilities, the ones that require a single talent point, they should
always be chosen, and if they have pre-requisites, those prerequisites should
also always be chosen as well.

going to sleep. Let's pick this back up tomorrow. Goodnight!

--------------------------------------------------------------------------------

### Assistant Response 10

> Good night. Before you go, recording today's decisions in the issues (no new
> code) so we can pick up cleanly tomorrow.

Good night. Today's decisions are written into the issues and committed. I've
built nothing new since your message and nothing is left running.

**Short answers to your questions:**
- **Items:** yes, you can change items freely. The cache version is read when
  the server starts, so a change reaches players on the first login after the
  next install and restart.
- **Item model:** no. What an equipped item looks like comes from the client's
  own item table, keyed by the item's ID. The server can change the tooltip
  icon, but not the model.
- **Duplicate spell with a new ID:** the server can define new spells, and they
  work fine as hidden effects, auras and triggers. But a new ID the client
  doesn't know has no name, icon, cast bar or cost it can show, so it can't be a
  spell the player learns and casts. Nuts indeed.
- **Talent cap and the patch system:** it is in the patch system. B030 is a
  normal source patch, applied before each build and reverted after. The C++
  sits in `src/cpp-basic/` only because it's a whole new file rather than an
  edit to an existing one.

**Recorded for tomorrow (not built):**
- **Spell class restrictions (705):** your option B. They come off every spell,
  and which trainer windows a character can open becomes the only gate.
- **Talent cap (155g):** tier 6 keeps only its capstone; the talents beside it
  and every deeper row stay refused.
- **Outland dungeons (155f):** +4 levels capped at 64, replacing the flat 64.
- **Bot talent plans (new 617g):** the 2/3–1/3 and three-thirds plans, a
  balancing pass run until every talent is used about equally, single-point
  abilities and their prerequisites always taken, and each buddy keeping one
  plan for life.

**Two questions to start with tomorrow:**
1. Stock Hellfire Ramparts creatures are about level 60–62, so +4 capped at 64
   makes almost all of them 64, not about 62. Did you mean +4 from each
   dungeon's entry level (55, so 59–63), or +2?
2. Those dungeons' blues are roughly item level 85–115, stronger than most of
   the original game's level-60 raid gear. Should they stay wearable at 60 (the
   difficulty is the gate), or go back to their stock required level?

--------------------------------------------------------------------------------

