# Conversation Summary: 8d2a3f68-514f-4a2a-873e-6f41da4eee36

Generated on: 2026-09-16 15:55:47
Models: claude-fable-5

--------------------------------------------------------------------------------

### User Request 1

hi, can you help me increase the number of bots? we should write a small script
or utility that can send cli messages to the server using loopback packets or
something similar. we should do this in response to the performance of the
computers that the server is running on - if they need 50% or more of the
computer's hardware specs, then decrease the number of bots logged in by logging
out those that are idling. each time, delete more bots, until the computer isn't
running so hot. Let the user adjust the thresholds for increasing and
decreasing, and give several enum style radio button choices for what logging
out method to use. Then, have another variable that's checkbox style that lets
the user select options for that specific behavior method.

--------------------------------------------------------------------------------

### Assistant Response 1

>  I'll help build a bot-population governor. Let me first look at the project's
>   current state — the new untracked files suggest related work is already in
>                                                                        flight.

                          Two research agents are now digging in the background:

       1. **Playerbot runtime control** — what console commands mod-playerbots
         exposes to change the live bot population without a restart, and how it
                                                            defines "idle" bots.
2. **Remote console plumbing** — how a local script can send commands into the
      running worldserver (SOAP on loopback vs. the RA telnet console), plus the
                           house style of `scripts/` so the new utility matches.

 Meanwhile I confirmed the current state: the vanilla profile holds the fleet in
a 128–256 band via the C020 config patch, and the highest phase-1 issue number
 is 150, so this will become issue 151. (The `new-issue-please-sort` note in the
     repo root is about renaming profiles — different topic, I left it alone.)

     Once the research lands I'll write the issue file first — a load-watching
  governor with adjustable increase/decrease thresholds, a radio-style choice of
    logout method, and per-method checkbox options — and then build the script
                                                                     against it.

--------------------------------------------------------------------------------

        *[background task] Agent "Find playerbot runtime commands" came to rest*

--------------------------------------------------------------------------------

### Assistant Response 1 (continued)

> Good findings from the first agent — the key lever is confirmed: `playerbots
>    rndbot reload` re-reads `playerbots.conf` live, so the governor can rewrite
>     Min/MaxRandomBots and nudge the server without a restart. One gap remains:
>       what the module actually does when there are *more* bots online than the
>     target (the logout path, and whether it already prefers idle bots — that
>  code fragment at line 1492 looks suspiciously like exactly that). Launching a
>                     focused follow-up while the remote-console agent finishes.

   No existing account tooling anywhere in the project — so part of this build
       will be a small self-contained SRP6 registration step (AzerothCore stores
 game-account passwords as SRP6 salt+verifier pairs, computable with `sha1sum` +
 `bc`, no Python needed) so the governor can mint its own gmlevel-3 SOAP account
                        instead of asking you to type console commands manually.

                                        The design is settling into four pieces:

       1. **Config patch (C021)** — flip `SOAP.Enabled = 1` on loopback in the
                                       installed worldserver.conf, all profiles.
 2. **Governor account setup** — a `setup` subcommand that inserts a dedicated
        admin account via SQL (SRP6 verifier computed in-script) and records its
        credentials via the existing `@CRED` mechanism in `scripts/credentials`.
   3. **The governor daemon** (`scripts/bot-governor`) — polls CPU and memory,
   sheds bots with escalating aggression while hot, regrows the fleet when cool,
          with your adjustable thresholds, a radio-style logout-method enum, and
                                                    per-method checkbox options.
      4. **Issue file 151** describing all of it, written before implementation.

  Still waiting on the third research agent tracing the module's logout path —
 whether mod-playerbots already prefers idle bots when shrinking the fleet (that
 determines which logout methods are real options versus wishful thinking). I'll
                                write the issue file as soon as it reports back.

--------------------------------------------------------------------------------

                  *[background task] Agent "Trace bot logout path" came to rest*

--------------------------------------------------------------------------------

### Assistant Response 1 (continued)

>   The full picture just landed, and it changes the design in an important way:
>     **mod-playerbots has no fast logout path**. Lowering the population target
>      only stops *new* logins — excess bots linger until their in-world timer
> expires (2 hours to 14 days). And `playerbots bot remove` refuses random bots.
>      So the governor must combine two levers: squeeze the band (conf rewrite +
>    `rndbot reload` so the module won't re-add), then actively kick chosen bots
> via the core `.kick` command over SOAP. That combination gives real, immediate
>    shedding — and the selection heuristics (idle, random, newest) become the
>                                                                 radio choices.
>
>  Now let me read the house-style files I'll be matching, then write the issue.

>          Now writing the issue file — the blueprint everything else follows.

>     Issue file written. Now the three implementation pieces — first the C021
>                                                                  config patch:

>                 Now the governor's config file with the radio/checkbox blocks:

> While the verification runs, the documentation pieces — the datapath doc and
>                                                   its table-of-contents entry:

       Documentation is in place: the datapath doc (`docs/bot-governor.md`), its
        table-of-contents entry, and placeholder `soap_user`/`soap_pass` keys in
               `scripts/credentials.dist` so fresh clones know those keys exist.

    The last piece before writing the governor script itself is the verification
  agent still running — it's confirming whether a kicked bot gets re-logged-in
by the module's per-bot bookkeeping while its in-world timer is still valid (and
    whether the module reads its event table from memory or from the database at
   runtime). The shed lever's exact shape depends on that answer, so I'm holding
                                                    the script until it reports.

--------------------------------------------------------------------------------

     *[background task] Agent "Verify kicked-bot relogin behavior" came to rest*

--------------------------------------------------------------------------------

### Assistant Response 1 (continued)

>     The verification came back with exactly the trap I feared — plus its own
> escape hatch. A kicked bot's "welcome timer" stays valid and the module *will*
>      re-login it via its offline sweep — **but that sweep only runs when the
>   online count sits below the module's target**. So the pin-then-kick order is
>  the whole trick: pin the band down first, then kick, and the count never dips
>    below target, so the rebound path never fires. Also confirmed: external SQL
>    writes to the module's event table are invisible (lazy in-memory cache), so
>  conf-pin + kick really is the only workable lever pair. Let me record that in
>                                            the issue, then write the governor.

>                                      Now the governor itself — the long one:

>         MySQL is live — good, the known-answer test is possible. Finding the
>                                                         account-creation site:

       Committed as `be0dd6f` — the governor is built, bench-verified, and one
       worldserver restart away from live duty. Here's the shape of what landed:

   **What it does.** `./scripts/bot-governor run` watches the machine's hardware
  meters every twenty seconds. When any selected meter reaches the hot threshold
  (default 50%), it sheds bots — and each consecutive hot cycle sheds twice as
   many as the last (8, 16, 32, up to a cap) until the machine cools. When every
   meter stays below the cool threshold (default 35%) for a sustained streak, it
 grows the fleet back — and its ceiling defaults to **512 bots, double the old
 static cap of 256**. That's how this increases your bot count: the fleet climbs
    whenever the hardware has room, because unlike a static config it can always
                                                                        retreat.

  **The discovery that shaped it.** The playerbot module has no fast logout path
   — it only ever logs bots *in*, and each bot stays until a personal timer (2
 hours to 14 days) expires. Worse, a forcibly disconnected bot gets re-logged-in
by the module's bookkeeping sweep — *unless* the population target has already
     been lowered, because that sweep only runs when the online count sits below
       target. So the governor pins the module's floor and ceiling to one value,
    reloads it live over the loopback console, and only *then* kicks. Pin first,
             kick second; the reverse order undoes itself within twenty seconds.

   **Your knobs** live in `config/bot-governor.conf`: both thresholds, the meter
     checkboxes (cpu / memory / load), the logout-method radio — `idle-first`,
  `random`, `newest-first`, or `drain` (no kicks, just squeeze and wait) — and
    the option checkboxes: `spare-grouped`, `spare-dungeoneers`, `spare-nearby`,
 `persist-shrunken-band`, `announce`. The script validates all of it and refuses
   to run on anything unrecognized, including spare options combined with drain.

   **What's verified.** The setup step already ran: it minted a `GOVERNOR` admin
         account by computing the login system's SRP6 cryptography in pure shell
(`sha1sum` + `bc`), and that math reproduces the exact verifiers of accounts the
 server itself created — plus the fresh account round-trips against its stored
 credentials, which is the same comparison the login handshake performs. All six
  kick-selection query shapes execute against the live schema, and the preflight
                passes everything except the one check that needs a live server.

**What's left for you:** restart the worldserver once (the console listener only
                     spawns at boot — the config is already flipped), then run
 `./scripts/bot-governor check` — it should go fully green — and then `run`,
  or `run --once` if you want to watch a single decision cycle first. Everything
 is written up in the issue file `issues/151-bot-population-governor.md` and the
                                            datapath doc `docs/bot-governor.md`.

--------------------------------------------------------------------------------

### User Request 2

we should only shed twice as many if the performance demands went up while we
were logging out the previous cycle's, if they stayed the same or went down then
we can do the same amount. never fewer, until we reach a point where removing
bots doesn't reduce performance demand several times in a row. the idea is to
create a day/night cycle, where all the bots that are idle are slowly logging
off for the night. then, they resurgence, and there's more of them during "peak
hours". This cycle might occur over the course of a real-world couple hours,
probably 3 or 4. this should make moments where it feels like there are few
bots, and moments where it feels like there are many. this behavior should be
off by default and only usable if there's no players online. meaning... to turn
down the performance requirements, you need to play the game teehee. oh and if
there's any spirits online, then the system is entirely paused, but only spirits
that have been online for a very long time like at least 15.

--------------------------------------------------------------------------------

### Assistant Response 2

>   Delightful design — the machine's day breathes like a world's day, and the
> only way to argue with it is from inside the game. Let me restate to make sure
>                                                       I build the right thing:
>
>    1. **Escalation becomes demand-responsive**: double the shed only if demand
>      *rose* since the previous shed; same-or-down keeps the same amount (never
>     fewer); and if several consecutive sheds fail to reduce demand at all, the
>     governor concludes bots aren't the problem and stops shedding for that hot
>                                                                       episode.
>        2. **A population tide** (off by default): when *zero* real players are
>    online, bot population follows a slow wave — floor at "night," ceiling at
>      "peak" — over a 3–4 real-hour cycle, idle bots drifting off gently on
>    the way down. While the tide runs, the load thresholds are dormant; logging
>        in suspends the tide and restores load-governing. That's the teehee: to
>                                                        cool the machine, play.
>    3. **Spirit pause**: any real player who has been a *ghost* for 15+ minutes
>    freezes the entire governor — a gameplay-level pause switch. (I'm reading
>         "spirits online" as dead-and-released player characters, detectable by
>                  their corpse's age; the threshold as minutes, both as knobs.)
>
>  First, the corpse table schema so spirit detection queries the right columns:

> Now the new observation functions and the tide math, added alongside the fleet
>                                                                     observers:

>                                       Now `do_tide`, inserted after `do_grow`:

> Now the main loop rework — mode arbitration (spirits > tide > load) plus the
>                                                         new escalation wiring:

           Committed as `cba4e83`. All three behaviors are in, bench-tested, and
                                                                     documented:

   **Result-listening escalation.** The shed amount now doubles only when demand
    actually *rose* since the previous shed; if it held or fell, the same amount
    goes again — never fewer within a hot episode. A ±2% deadband keeps meter
   jitter from counting as movement. And the futility brake you described: three
     sheds in a row (a knob) that fail to reduce demand at all, and the governor
  concludes the load isn't bot-shaped — a compile, some other service — logs
   that plainly, and stops shedding until the machine cools by itself. The bench
        simulation replays these exact stories: `8 16 16 16 16 32 gave-up` for a
      rise-hold-fall-hold-rise-hold sequence, `8 8 8 8 8` when every shed helps.

 **The day/night tide.** Off by default (`GOVERNOR_DAYNIGHT="off"`), and it only
breathes while *zero* real players are online. Population follows a sine between
    floor (128, the night) and ceiling (512, peak hours), one full cycle every 3
 real hours — the knob takes up to 24. The pin drifts at most 8 bots per cycle
  in either direction, so the ebb is idle bots slowly wandering off to the inns,
       not a stampede, and the wave is anchored to the wall clock so a restarted
 governor rejoins the tide mid-phase. While it runs, the hot/cool thresholds are
 deliberately dormant — which is where your teehee lives: the only way to turn
the performance demands down is to log in, because a player online dethrones the
                                               tide and restores load-governing.

 **The spirit pause.** Any real player who dies, releases, and stays a ghost for
15+ minutes (knob: `GOVERNOR_SPIRIT_MINUTES`) freezes the entire governor — no
   shedding, no growing, no tide — until they return to their body or log off.
    Detection is by corpse age: the corpse table keeps exactly one resurrectable
        body per character stamped with its death time, so corpse age *is* ghost
      duration. Which makes this an in-game hold switch you operate by dying and
                                                                        waiting.

Two judgment calls worth flagging in case I read you differently than you meant:
 I took "spirits" to be dead-and-released *player* characters and the "15" to be
 minutes; and I took "only usable if there's no players online" to mean the tide
fully suspends the load thresholds while it runs (rather than hot-shedding still
 overriding the wave). Both are single-line changes if you had something else in
                                                                           mind.

   `status` now reports the tide's current wave position and any spirits holding
  the world. The remaining step is unchanged: restart the worldserver once, then
                           `./scripts/bot-governor check` should go fully green.

--------------------------------------------------------------------------------

