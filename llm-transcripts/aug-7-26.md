# Conversation Summary: 57696e7f-b993-4f20-a684-74306e2a2f12

Generated on: 2026-09-16 17:59:16
Models: claude-opus-5

--------------------------------------------------------------------------------

`/model` - Set model to Opus 5 (1M context) and saved as your default for new
sessions

--------------------------------------------------------------------------------

### User Request 1

hi, can you read the issue files and todo files in this directory and sort them
into issue files? Be sure to check both issues/ and issues/completed/ just
incase we already did. We can then remove the todo files once they're suitably
captured in the issue files.

--------------------------------------------------------------------------------

### Assistant Response 1

> I'll start by exploring the directory structure to find the todo files and
> existing issues.

> I found several loose files. Let me read them all.

> Let me check for other loose note files and look at the existing issues that
> might already cover these.

> The `todo` and `todo-urgent` are tracked; the `new-issue*` files are
> untracked. Now let me check which existing issues already cover this material.

> Let me gather the remaining context I need — what "unlock quests" refers to,
> and the equipment-creation mechanism.

> I have the full picture now. Writing the issue files.

> Now re-opening 110 for the CLAUDE.md dedup pass. First let me see what
> actually still overlaps.

> Now preserving the writing from the todo files, following the
> `claude-md-asides.md` precedent.

> Now updating the phase progress files and the table of contents.

> Now linking the new issues back into the ones they change.

> Everything is captured. Removing the loose files now.

> Everything from the five loose files is now in issue files, and the files are
> gone. Here's what landed where.
>
> **New issues**
>
> - **148v — cloned kit items render invisible.** Your hypothesis was right:
>   148h clones each kit item into a new `item_template` row at `original_entry
>   + 2000000` and retunes the clone. But the fix you propose (modify in place)
>   is the thing 148h deliberately backed away from — in-place editing of
>   Polished Scale Vest changes it for every mob that drops it, every vendor,
>   every quest reward, world-wide. That leak already happened once. So the
>   issue writes up two candidate causes and one query that tells them apart: if
>   the clones' `displayid` matches their originals, the cause is the client's
>   `itemcache.wdb` never having seen entries in the 2000000 range, and the fix
>   is timing inside the first-login hook. If `displayid` is zero, the clone
>   INSERT dropped a column and it's a one-line fix. The warrior/paladin
>   difference is evidence for the first — those clones were already cached
>   from earlier rerolls. Deleting `itemcache.wdb` and rolling a warrior is the
>   discriminating test.
> - **148w — mage has no spells at level 20.** Defect against 148j. The
>   sharper finding is a contradiction: `scripts/validate-vanilla-starter-state`
>   reports the 148j check passing while the client shows an empty spellbook.
>   The validator checks that rows exist, not that coverage is complete — a
>   mage with three rows passes. One query (`COUNT(*) GROUP BY class`) splits
>   this into "generation query under-selected mage" versus "migration never
>   reached the live DB."
> - **152 — rename vanilla→basic, release→expert.** Includes the
>   directory/database/config-gate blast radius and the warning that `release`
>   also appears as an English word and as CMake's build type, so that sweep
>   needs three buckets not one. Your alpha/beta versioning note from
>   `todo-urgent` is in there too, because it exposes that `alpha` and `beta`
>   each mean two different things in this project — a build target and a
>   release stage.
> - **412 — dynamic questing prototype.** Your unlock-quest design, whole.
>   It's blocked by 210, which already names this exact feature as the thing it
>   unblocks.
> - **918 — fg-sora-cluster.** Three mirror bots, one per box. "Two truths and
>   a lie" is written up as the acceptance test rather than a feature, because
>   passing it requires shared state, agreement, divergence, and turn-taking all
>   working at once — and which part it fails tells you which is broken.
>
> **Updated:** 110 reopened for the CLAUDE.md dedup (the overlaps are catalog
> entries 146, 633–634, 638–646) and moved back out of `completed/`; 701
> records the reversal; 136, 148o, 916 got cross-references; TONIGHT.md archived
> per `phase-structure.md`'s own disposition. The philosophy and the "feldowinn,
> chorus of angels" line are preserved verbatim in `notes/todo-file-asides.md`.
>
> Four questions I couldn't settle on your behalf:

> **[Asked the user]**
>
> *Q1 — 412 phase:* 412 (dynamic questing) is filed in phase 4 as you asked,
> but the fit is loose — phase 4's effect is "loot circulates." Only the
> forced-loot mechanic is really phase 4; the giver rides the traveler stream
> (phase 5) and the monsters substitute into the ambush queue (phase 3).
> - Keep it in phase 4 — As directed. The justification written into
>   phase-4-progress.md is the loot-policy contrast: treasure is deliberately
>   un-ownable to force cooperation, quest items are deliberately un-stealable
>   so a group can't eat someone's objective. Two opposite policies in one
>   phase.
> - Give dynamic questing its own phase — Phase 11. It's a system that spans
>   three existing phases and the prototype is explicitly the first of a general
>   series, which is the shape a phase usually has here.
> - Move it to phase 5 — Encounters. The traveler stream is the entry point
>   for both the giver and the turn-in, and 503's dynamic trainer spawning is
>   the closest existing precedent.
> → **Selected:** Keep it in phase 4
>
> *Q2 — Rename scope:* 152 renames vanilla→basic and release→expert. Your
> note didn't mention the other two profiles.
> - Leave alpha and beta alone — Profile set reads basic / expert / beta /
>   alpha — two names from D&D, two from release stages. Defensible, since
>   beta and alpha genuinely are staging targets rather than player-facing
>   experiences. This is what 152 currently assumes.
> - Rename all four — A full scheme with no mixed vocabulary. Would also fully
>   resolve the alpha/beta stage-versus-target collision that 152 flags, instead
>   of resolving one of three.
> - Rename beta too, keep alpha — Beta is the one that most often gets
>   confused with the release stage, since it's an active target. Alpha is a
>   frozen relic nobody talks about day to day.
> → **Selected:** Leave alpha and beta alone
>
> *Q3 — Diagnose now:* 148v and 148w each come down to one SQL query against
> acore_world_vanilla. Want me to run them now and turn the guesses into
> answers?
> - Yes, run both checks — Clone displayid comparison for 148v, per-class
>   pretrain row count for 148w. Read-only queries. Would collapse most of the
>   speculation in both issues into a stated cause, and I'd update the issues
>   with what came back.
> - No, leave them as written — The issues stand as diagnosis plans with the
>   discriminating test spelled out. You or someone else runs them when the
>   server's up.
> → **Selected:** Yes, run both checks
>
> *Q4 — Concept map:* docs/concept-issue-map.md is supposed to be updated when
> new issues are created, but it currently covers about a dozen issues out of
> roughly 150 — it looks abandoned rather than merely behind.
> - Leave it, flag it — Adding six entries to a doc that's already 90%
>   incomplete makes it look more current than it is. I'd rather tell you it's
>   stale than partially patch it.
> - Add the six new issues — Follows the CLAUDE.md rule literally. Doesn't fix
>   the existing gap.
> - Rebuild it properly — Separate piece of work — sweep every issue against
>   the catalog and regenerate the map, ideally with a script so it stops going
>   stale.
> → **Selected:** Leave it, flag it

> Running the two checks now.

> Restoring those rather than leaving them deleted.

> All five archived, byte-for-byte identical to the originals, and git recorded
> the two tracked ones as renames so their history follows them. Let me correct
> the note that said they were removed, and add a README.

> Now the two diagnostic queries.

> The project's MySQL isn't running (the `mysqld` I found is an unrelated system
> instance without the vanilla databases). Starting the project one — it's the
> documented precondition and `scripts/stop-mysql` reverses it.

> Mechanism B ruled out — all 51 clones carry their originals' `displayid`.
> Now the mage check.

> The schema uses **bitmasks**, not class IDs — 148j's design specified
> `race`/`class` columns that don't exist. Let me see what actually got written.

> All powers of two — the generator wrote proper bitmasks. Mage is `classmask`
> 128: **123 rows, the most of any class.** So the data is there. Let me check
> what the characters actually know.

> Only 453 of 23,915 characters have any spells — and the newest mages have
> none. Let me see where the line falls.

> The two populations interleave across the whole guid range — so it's not a
> before/after cutoff. Checking which creation path each went through.

> Now the picture resolves. Let me confirm which accounts are bots.

> Account 101 is **RITZ** — and guid 22816 "Kaersenon," Blood Elf Mage, level
> 20, is on it with **2** spells. That's the reported character. One more query
> to pin the cause.

> Definitive. Let me record both findings in the issues.

> Correcting 148j's schema documentation, which was the source of the
> wrong-column assumption.

Done. The five notes are in `issues/completed/archived/` — byte-for-byte
identical to the originals, with `todo` and `todo-urgent` recorded by git as
renames so their history follows them. There's a README there explaining that
these sit at the same layer as the transcripts: input that issues were compiled
from, not issues themselves. `notes/todo-file-asides.md` now says archived
rather than removed.

**The two checks you approved both came back conclusive, and neither cause was
what the reports assumed.**

**148v — the invisible mage gear.** All 51 cloned kit items carry their
originals' `displayid`, none are zero, none differ. The clone SQL is correct. So
the answer to your question is: yes, we're copying items, but copying isn't what
broke it, and modifying in place would trade a cosmetic bug for the world-wide
leak that 148h was built to escape. What's actually happening is that the clones
live at entry `original + 2000000`, which no WoW client has ever seen. The
client keeps item metadata in `WDB/itemcache.wdb` and has to ask the server for
anything not in there. Until that round trip lands, the paperdoll has an item
with no display ID — nothing to hang a model on, so it draws the red "?". Your
unequip/re-equip forced a re-read after the answer arrived. Warriors and
paladins looked fine because their clones were already cached from earlier
rerolls; delete `itemcache.wdb` and a warrior will do it too. The fix is timing
inside the first-login hook, not data.

**148w — the spell-less mage.** The pretrain data is complete and correct: 717
rows, mage has 123 of them, more than any other class. Your character is
Kaersenon, guid 22816, Blood Elf Mage on account `RITZ`. She should have
received 123 spells. She received **zero** — the two rows in her spellbook are
spell 201 and 1180, which are the One-Handed Swords and Daggers weapon
proficiencies from the kit hook, not spells at all. So "no spells to cast" was
literal.

It isn't mage-specific either. Of 23,915 characters, only 453 have a single
spell between them, and those sit almost entirely below guid 450. The other
23,462 — the whole bot fleet — have empty spellbooks.

The likely cause is that the engine reads `playercreateinfo_spell_custom` from a
cache built at worldserver startup, so rows applied to a running server don't
take effect until restart, and characters created in between are permanently
short. 148o already documents this exact behavior for the neighboring
spawn-coordinate table. The test is a worldserver restart plus a fresh mage: 123
spells means it was only ever a missing restart.

I also corrected 148j, which documented the target table as having `race` and
`class` columns. It doesn't — it's `racemask`/`classmask` bitmasks, and that
distinction bites hard: writing class 8 into a mask column sets bit 3, which
silently grants a mage's spellbook to rogues. The generator got it right, but
the doc would have led the next person wrong.

Two things needing you:

**I started the project MySQL** to run these — it wasn't running, and the
`mysqld` that was is an unrelated system instance without the vanilla databases.
It's up on port 3307 now; `scripts/stop-mysql` if you want it down.

**I haven't committed.** The tree has a lot of work that isn't mine — modified
`scripts/`, `patches/`, `config/patches/`, `CLAUDE.md`,
`installed-files-release/`, plus untracked 917, 1101, 308, B026/B027 and
`debug-run.sh`. Mine is the six new issue files, the archive directory,
`notes/todo-file-asides.md`, and edits to 110, 136, 148j, 148o, 701, 916, the
four phase-progress files, `phase-structure.md`, and
`docs/table-of-contents.md`. Want me to stage just those, or is the other work
yours to commit alongside?

--------------------------------------------------------------------------------

### User Request 2

> The likely cause is that the engine reads playercreateinfo_spell_custom from a
> cache built at worldserver startup, so rows applied to a running server don't
> take effect until restart, and characters created in between are permanently
> short. 148o already documents this exact behavior for the neighboring
> spawn-coordinate table. The test is a worldserver restart plus a fresh mage:
> 123 spells means it was only ever a missing restart.


This won't work. The worldserver has been restarted several times inbetween
character creations.

  148v — the invisible mage gear. What's actually happening is that the clones
  live at entry original + 2000000, which no WoW client has ever seen.

this also isn't correct. The issue is that any ID that hasn't been seen isn't
sent until an event occurs. You're right, and also wrong, which happens a lot to
anyone thinking through a problem. The answer is we have to go back and edit the
original ID's, sadly. Can you update the relevant issue files?

> I haven't committed. The tree has a lot of work that isn't mine — modified
> scripts/, patches/, config/patches/, CLAUDE.md, installed-files-release/, plus
> untracked 917, 1101, 308, B026/B027 and debug-run.sh. Mine is the six new
> issue files, the archive directory, notes/todo-file-asides.md, and edits to
> 110,
> 136, 148j, 148o, 701, 916, the four phase-progress files, phase-structure.md,
> and docs/table-of-contents.md. Want me to stage just those, or is the other
>   work yours to commit alongside?


As always, just stage your own work, breaking files up by line if there's things
that are unrelated. I've written this in the CLAUDE.md file, are you having
trouble finding the instructions or anything like that?

--------------------------------------------------------------------------------

### Assistant Response 2

> Let me check something on the spell side before rewriting — there's a config
> gate I should have checked first.

> `LearnCustomSpells()` is gated. Let me find the config key and its current
> value.

> The key is **`PlayerStart.CustomSpells`**, default `false`. Checking the live
> vanilla config.

> **Found it — and it's not a timing problem at all.**

> Now trimming the hypothesis list in 148w that the confirmed cause makes
> obsolete.

> Now 148v — recording your call and reversing the clone design.

> Now recording the reversal in 148h, which owns the clone design.

> Clean split — every file I touched was untouched by the other work, so no
> line-level splitting was needed. Committing.

Both corrections were right, and chasing the first one down found the actual
cause of the second.

**148w — the restart theory was wrong, and so was every other theory in that
issue.** The pretrain table isn't stale, isn't mis-masked, and isn't unapplied.
It has never been read. `Player::LearnCustomSpells()` opens with an early
return:

```cpp
if (!sWorld->getBoolConfig(CONFIG_START_CUSTOM_SPELLS))
    return;
```

That constant binds to `PlayerStart.CustomSpells`, which `WorldConfig.cpp:550`
defaults to `false`, and `installed-files-vanilla/etc/worldserver.conf:1699`
reads `PlayerStart.CustomSpells = 0`. No config patch sets it. So all 717 rows
are inert — the function returns two lines above the loop that would read
them, on every character creation, on every boot. That's why restarts changed
nothing, why mage wasn't special, why 23,462 characters have empty spellbooks,
and why the two things Kaersenon *does* know are weapon proficiencies from our
own Lua hook, which isn't gated by that flag. The fix is one `C*.sh` config
patch, and it has to be a patch rather than a hand-edit because the conf is
regenerated from `.dist` at install.

**148v — you're right that the round-trip framing was only half the story.**
The client doesn't fetch lazily in the background; it fetches when an event
prompts it, and a server-side hook quietly equipping gear isn't one. So there's
no server-side fix — every workaround is a way to trick the client into
generating an event, which is timing-dependent and fails invisibly. I've
recorded the decision to go back to the original IDs in 148v and marked 148h's
cloning procedure as reversed, with the reversal ordered so world references
come home before the clones are dropped.

One thing that came out of writing it up, which makes the call cleaner than
"sadly": **the clones never contained the leak they were built for.** 148h's own
loot sweep rewrites thirteen tables in `acore_world_vanilla` to point at the
clones, so the retuned values already apply to every drop, vendor, and quest
reward in the vanilla world. The clone bought a different entry number for an
identical outcome — and that number is exactly the part the client can't draw.
In-place isn't a regression on the leak axis; it's the same world-wide retune
with an entry every client already has. What it genuinely costs is
reversibility, so 148v calls for the apply SQL to record each entry's original
values, since 148h notes the last in-place unapply was a guessed soft revert.

**Committed** as `42a2c69`, staged file-by-file. Nothing needed line-splitting
— every file I edited was clean before I touched it, so no commit of mine
straddled someone else's work. Your modified `scripts/`, `patches/`,
`config/patches/`, `CLAUDE.md`, `installed-files-release/`, and the untracked
917/1101/308/B026/B027 work are all exactly as they were. The pre-commit hook
bundled the transcripts as designed.

On your question: no, I found the instruction — "only commit the changes for
the specific functionality you touched" is clear. I asked because a different
line says that when modified files aren't mine I should work out what issue they
belong to and *ask before committing*, and I read the volume of foreign changes
as triggering that one. Those two pull opposite ways when the answer is always
"just stage mine." I'll take the standing answer and stop asking.

Two loose ends: **the project MySQL is still running** on 3307
(`scripts/stop-mysql` to stop it), and the table-of-contents entry I added for
`notes/claude-md-asides.md` points at a file that's still untracked in your
working set — it'll resolve when you commit it.

--------------------------------------------------------------------------------

### User Request 3

question about 148v - can we write some kind of lua script or something that
runs on the first login of a character that updates all the records we've
touched? We can specify them specifically if needed, but the idea is to FORCE an
update when there's stuff we've changed. Something like... adding an item to a
backpack, equipping that item, unequipping that item, removing the item from the
backpack, then doing that process to EVERY ITEM WE PATCHED, then when that's
done, replacing it with what's supposed to go there. Is that... absurd? would it
work? The goal is no more question marks.

--------------------------------------------------------------------------------

### Assistant Response 3

> Not absurd at all — it's a known private-server technique, and there's a
> much cheaper version of it. But the mechanism isn't quite what you described,
> and the difference decides whether it works. Let me check what ALE actually
> gives us first.

> `Player:SendPacket` and a global `GetItemLink` both exist. Let me check
> whether we can build packets from Lua.

Not absurd — and ALE gives us more than you'd need. `CreatePacket` and a full
`WorldPacket` class exist, plus `Player:SendPacket` and a `GetItemLink` global.
So all versions of this are buildable.

But your model of the failure is slightly off in a way that changes the design.
**The client isn't failing to get the data. It's getting it too late and never
redrawing.**

When an item entry the client doesn't know shows up, the client *does*
automatically fire `CMSG_ITEM_QUERY_SINGLE` and the server answers. That happens
on its own. The problem is that your character model is built at world-enter
from whatever display IDs are on hand at that instant — which is none — and
when the responses land a few hundred milliseconds later, nothing invalidates
the already-drawn model. The data arrives; the render already happened.

That's why the manual fix worked and why it had to be manual: unequip/re-equip
does *two* things, and only the second one is the actual repair. By the time you
dragged the item, the cache was already warm from the automatic query.
Re-equipping just forced a model rebuild.

So the bug has two halves, and any fix needs both:

**Half one — warm the cache.** Three ways, cheapest first:
- **Item links.** `GetItemLink(entry)` into a chat message the player never
  really sees. Rendering a link requires the template, so the client queries on
  miss. A handful of lines, no bag space, no inventory churn. This is the
  standard private-server trick.
- **Your inventory cycle.** Works, but each item has to survive at least one
  update tick or the object create/destroy may coalesce and the client never
  sees it — so it's not a tight loop, it's 51 items each needing a beat. And
  51 items won't fit in a backpack at once.
- **Hand-built query responses.** `CreatePacket` an
  `SMSG_ITEM_QUERY_SINGLE_RESPONSE` per entry and push it unprompted. No round
  trip at all, fully deterministic — but that packet is ~130 fields in exact
  order, and one wrong field corrupts everything after it.

**Half two — force the redraw.** Warming alone changes nothing on a model
that's already on screen. You still need to dirty the visible-item fields, which
is what the equip cycle actually accomplishes.

Here's what makes me not want it as the fix for the kit, though. `itemcache.wdb`
is **per client installation and persists on disk** — not per character. Which
means the thing you'd be compensating for isn't a property of the character at
all, and `PLAYER_EVENT_ON_FIRST_LOGIN` would be the wrong hook: it fires once
per character, while a player who wipes their WDB, or logs in from a second
machine, would be right back to question marks on a character that already used
up its one firing. To be safe it has to run every login, forever, on all 51
entries, to compensate for using entry numbers no client knows — when the
entry numbers every client already knows are free.

**Where this absolutely is the right answer: issue 405's Ability Tomes.** Those
are entries 900001+, genuinely new items with no canonical original to fall back
on. There's no in-place option there. The warming mechanism isn't a workaround
for tomes, it's the only way they'll ever render — and 405 is currently
written as though that problem doesn't exist.

So my recommendation is: keep the revert to original IDs for the kit, and build
this as a general "make the client aware of a custom item" utility aimed at 405,
where it earns its keep.

One thing I'd like your read on, since it's the part I'm least sure of. Given
the cache is per-client and lives on disk — if we do build the warming
utility, what should trigger it? Every login is safe but wasteful. Is there
something the server can observe that tells it *this particular client* is cold,
so it only pays the cost when it matters? I can't find a clean signal for that,
and I'm not sure one exists.

--------------------------------------------------------------------------------

### User Request 4

Hmmmmm alright let's leave this alone for now. How about we just do the edits
that modify the current ID, and accept a little bit of tainted "required levels"
on some white items that nobody's going to care about past their starting
equipment? That should work without messing with custom packets, and it should
(this is the main reason I want to do it this way) it should display on the
character select screen before they've logged in for the first time, which is
important.

--------------------------------------------------------------------------------

### Assistant Response 4

> Before I start — one correction, because you named the select screen as your
> main reason and it's the one part that argues differently than you'd expect.
>
> The character-select screen doesn't use the client's item cache.
> `SMSG_CHAR_ENUM` carries the display ID for each equipped slot **directly in
> the packet** — the server reads `item_template.displayid` itself and writes
> it in. So the select screen was most likely rendering the clones correctly all
> along; the "?" and the invisible model were in-world problems only.
>
> That doesn't change the decision, and I'm not relitigating it — in-place is
> still right, for the reasons that do hold: the redraw half of the bug affects
> in-world rendering no matter how warm the cache is, every other player's
> client has to learn 51 novel entries before bots render, and the alternative
> is a warming routine running on every login forever. Just don't let the select
> screen carry the weight, because it'd have been fine either way. Cheap to
> confirm once the change lands.
>
> Let me look at what implementing this actually touches.

> The E018 patch function is a generic copy-apply/copy-revert, so this needs no
> change to `patches/E-patches.sh` — which is good, since that file has your
> uncommitted work in it. I only need to rewrite the two SQL source forms and
> the generator.

> Now the generator, which has clone IDs baked into its tables.

> 128 occurrences — I'll transform them mechanically rather than by hand.
> First, checking for false positives.

> All clean — every 7-digit number is a clone ID, plus the literal offset
> `2000000` in comment text, which I'll preserve.

> The IDs are right. Now the comments that still describe cloning.

> Now regenerating the kit SQL from the updated generator.

> Same in both, so that's pre-existing, not a regression. Spot-checking the
> regenerated rows.

> Correct throughout. Updating 148v's implementation steps to reflect what's
> built versus what still needs your server.

> Not tracked — the active SQL files are build artifacts the patch copies from
> `.src`, so the source forms are the whole commit.

Done — committed as `8b0ef7b`. The kit now references canonical entries and
E018 tunes those in place.

**What changed.** The patch runner needed no edit — `patch_E018_*` is a
generic copy-apply/copy-revert, so rewriting the two source forms was the whole
thing. That also kept me out of `patches/E-patches.sh`, which has your
uncommitted work in it.

The apply form is now V3 in four steps. Step 0 undoes the V2 clone state if it's
still live — reverse-sweeps all thirteen reference tables plus
`playercreateinfo_item` by subtracting the offset, then deletes the clones. It's
keyed on the `original + 2000000` convention rather than a hand-typed map, so
there's no second copy of the entry list to drift out of sync. That makes the
file converge the same way from a V2 database, a V3 database, or a fresh import.
Steps 2 through 4 then cap required level and normalize damage on the originals,
carrying the V2 numbers over unchanged — those were never wrong, only written
to the wrong entry.

**On the taint you accepted, it's smaller than you agreed to.** The
required-level cap is guarded by `WHERE RequiredLevel > 20`, so it only touches
items that actually block a level-20 character from equipping. The Hearthstone,
the bags, the arrows, the flowers, and the Battle Axe were already at or below
20 and are left exactly as upstream shipped them. And 923 Longsword, 927 Double
Axe, 928 Long Staff, and 2209 Kris aren't touched at all — V2 only cloned
those so V1's leak could be reverted on them, and they're not in the current
kit.

**The one thing the clones genuinely did better, now handled properly.**
In-place edits have no natural undo, and 148h records that the V1 back-out
"re-floored to RL=22" from a hand-typed table and was known imperfect. V3
snapshots `RequiredLevel`, both damage fields, and the stat pair for all 47
entries into `_vanilla_kit_original_values` before writing anything, with
`INSERT IGNORE` so a re-apply can't overwrite the baseline with tuned values.
The revert restores from that by JOIN. One limitation I wrote into the file
rather than hiding: damage fields are genuinely pristine since neither V1 nor V2
ever wrote damage to an original, but required-level on the ~19 items V1 once
touched is second-hand — V2 restored those from its own hand-typed table, and
that's what got captured. A true reference for those needs a fresh world import.

The generator's 125 clone IDs converted mechanically rather than by hand, and I
verified every 7-digit number in the file was a clone ID before running it.
Regenerated output is 581 item rows across 52 combos with nothing left in the
2000000 range.

**Needs your server**, listed in 148v: reinstall so the patch copies the new
source forms into place, boot so UpdateFetcher re-hashes both changed
migrations, then confirm the clone range is empty, the snapshot holds 47 rows,
and the kit draws on a client with `itemcache.wdb` deleted.

Still outstanding from earlier and unrelated to this: 148w needs its one-line
config patch for `PlayerStart.CustomSpells`, and the project MySQL is still
running on 3307.

--------------------------------------------------------------------------------

### User Request 5

great let's fix 148w. when you're done you can stop the mysql server.

--------------------------------------------------------------------------------

### User Request 6

Base directory for this skill: /home/ritz/.claude/skills/upstream-patch-system

# Upstream Patch System

A method for owning customizations to code you didn't write, without forking it.

> **This document describes machinery to build, not a procedure to follow.** It
> assumes nothing about which agent, editor, language, or runtime executes it.
> Everything below is plain shell and text-processing logic — `git`, `sed`,
> `grep`, `awk`, traps, generated artifacts — that any LLM or human can
> assemble
> in any project. Swap equivalent tools freely.

---

## Principle: machinery, not procedure

A **procedure** is a sequence an operator (human or LLM) must work through by
hand. A **machine** is a set of components wired together so the outcome falls
out
deterministically, with no operator memory in the loop. This system is built
entirely as machinery, on purpose:

- **Every "remember to…" is a defect.** Anything that depends on the operator
  recalling a step is a latent bug. Convert it into one of four mechanical
  forms:
  a **guard** (a precondition check that makes the operation a no-op when it
  shouldn't fire), a **trap** (cleanup wired to process exit so it runs even on
  failure), a **generated artifact** (a derived file that can't drift because
  it's
  recomputed, never hand-edited), or a **gate** (a hard stop that refuses to
  proceed until an explicit condition or approval is satisfied).

- **The only inputs left to a human are irreducible.** Three, specifically: the
  *transformation itself* (the old→new text edit and the anchor that locates
  it),
  the *judgment that a given patch belongs upstream*, and the *approval of an
  outward-facing action* (opening a PR, deleting a fork). Everything else —
  when
  patches apply, when they revert, whether they still match, what the registry
  says, whether the tree is clean — is wired and deterministic.

Read every section below as "here is a component and how it is wired so the
guarantee holds without anyone watching," not "here are steps to perform."

---

## The core problem this solves

You depend on a third-party codebase — an upstream project, a vendored
library, a
submodule. You need to change it: fix a build error against your toolchain, add
a
hook, silence warnings, tune a constant, adjust a behavior. The two naive
approaches both rot:

- **Edit the source in place.** Edits get clobbered when you pull upstream, or
  they silently survive and are forgotten. The tree drifts and nobody can tell
  your changes from upstream's.
- **Maintain a hard fork.** Every upstream update is a merge conflict. The fork
  diverges, the diff grows opaque, and "what did we change and why" becomes
  unanswerable.

The machine takes a third path: **the cloned source tree is a disposable build
artifact.** It is never committed and never the source of truth. The source of
truth is a directory of small scripts, each of which knows how to *apply* one
customization and how to *reverse* it. On every build the machine stamps the
changes into a freshly-cloned tree and peels them back off afterward; the tree
round-trips to a pristine copy of upstream HEAD.

What you version is the **intent** (the apply/unapply scripts and their
headers),
not the **result** (the modified source). The result is a deterministic function
of `upstream + scripts`, regenerated on demand.

## What the machinery buys you

- **Upstream updates stop hurting.** Re-clone, re-apply. A patch that no longer
  matches fails loudly with an unmatched pattern — a *signal* the machine
  surfaces
  automatically, pointing at the one patch to re-target, rather than a conflict
  to
  resolve across the tree.
- **Every change is documented at the point of change.** Each script's header
  records the upstream symptom, the pattern it matches, and how it reverses —
  and
  the registry is *generated* from those headers, so "what did we change and
  why"
  is always answerable and never stale.
- **The diff is legible.** N upstream problems become N small, named, anchored
  edits, not one sprawling fork diff.
- **Changes are portable.** A patch script (or its prose "patch doc") replays
  onto
  any compatible upstream. You ship knowledge, not binaries.

---

## Mental model: a patch is a reversible function over a text tree

Each patch is a **pair of functions** over the source tree:

```
apply:    pristine_tree    ->  customized_tree
unapply:  customized_tree  ->  pristine_tree     (exact inverse of apply)
```

Two properties, both enforced mechanically rather than by discipline:

1. **Idempotent.** `apply` twice equals `apply` once; same for `unapply`. The
   enforcement is a *guard*: apply checks "is the un-patched pattern present?"
   before editing; unapply checks "is the post-patch shape present?" before
   reverting. An already-applied patch is a silent no-op — not because the
   operator avoided re-running it, but because the guard makes re-running inert.

2. **Exact-inverse.** `unapply(apply(tree)) == tree`. The unapply transformation
   is the literal mirror of the apply transformation. This is what lets the tree
   round-trip — and it is *asserted* by the verify harness (below), not
   trusted.

---

## The tiers: components separated by *when they fire* and *what they target*

Customizations fire at different moments and touch different trees, so the
machine
groups them into tiers keyed to the **chronological phase of the build**. Each
tier is a driver function; a project wires up as many as its chronology requires
(often fewer than three).

```
PRE-BUILD   (source patches)    before compile   edits the SOURCE tree (code you don't own)
POST-BUILD  (install patches)   after install    sets up the INSTALL tree (configs, assets, schema)
POST-DEPLOY (config patches)    after promote     tunes the LIVE config (paths, secrets, runtime knobs)
```

| Tier | Fires | Target | Job | Reverts? |
|------|-------|--------|-----|----------|
| **PRE-BUILD** | after clone, before compile | upstream source files | make upstream build/behave for you (fixes, hooks, warning silencing, behavior tweaks) | **Yes** — must revert so the source stays pristine |
| **POST-BUILD** | after install-to-staging, before validate | the just-installed staging tree (configs from templates, log dirs, symlinks, schema/data) | set up a freshly-installed instance | usually no (re-run is idempotent) |
| **POST-DEPLOY** | after promoting staging to live, before start | live config files + runtime data rows | write deploy-specific values unknowable earlier: which database, which paths, which ports, plus runtime tuning | no |

**The separation is chronological because each tier has a precondition the prior
phase establishes.** PRE-BUILD needs the source present but the binary not yet
built. POST-BUILD needs the binary installed into staging, though the live
directory may not exist. POST-DEPLOY needs the live directory to exist (promote
created it) so it can write paths/credentials pointing *at* it — values that
did
not exist at any earlier phase. The tiers cannot be collapsed without violating
a
precondition. A side benefit of wiring runtime-tuning (POST-DEPLOY) *after* the
validation gate: a botched tuning value cannot fail validation, because
validation
runs against complete, valid staging configs from POST-BUILD; POST-DEPLOY writes
only the *delta* between "valid for staging" and "valid for live."

**PRE-BUILD is the load-bearing tier** — the only one touching code you don't
own,
hence the only one whose revert must be exact. POST-BUILD and POST-DEPLOY touch
*your* artifacts and behave like ordinary idempotent setup. Wire only the tiers
your chronology demands.

---

## Directory layout (the parts bin)

```
project/
├── patches/
│   ├── patches.sh                  # the orchestrator: sources every patch,
│   │                               #   declares per-profile patch lists,
│   │                               #   defines the apply/unapply/audit drivers
│   ├── P001-<short-name>.sh         # one PRE-BUILD patch: apply + unapply pair
│   ├── P002-<short-name>.sh
│   ├── I001-<short-name>.sh         # POST-BUILD (install) patches
│   └── C001-<short-name>.sh         # POST-DEPLOY (config) patches
├── scripts/
│   ├── scaffold-patch               # emits a new patch skeleton (generator)
│   ├── verify-patches               # asserts round-trip + match-count invariants
│   ├── gen-registry                 # regenerates the registry from patch headers
│   ├── redownload-source            # wipes the source tree for a fresh clone
│   ├── install                      # clones upstream + submodules, then compiles
│   ├── compile                      # the build driver (apply → build → unapply)
│   ├── apply-patches                # operator surface over the drivers
│   └── audit-patches                # the pruning machine (post-update)
├── docs/patches/
│   ├── patch-registry.md            # GENERATED index — never hand-edited
│   └── <feature>.md                 # optional prose "patch docs"
├── .upstream-lock                   # commit last successfully built against
└── source/                          # THE CLONED TREE — gitignored, disposable
```

> ID prefixes (`P`/`I`/`C`) are arbitrary; the rule is one tier per prefix,
> sequential numbering, so a glob selects a tier deterministically.

The load-bearing line of the whole design lives in `.gitignore`:

```gitignore
source/
```

The cloned source is **never tracked**. A surviving modification to upstream
code
in your repo's history is, by construction, a bug — a failed unapply or a
manual
edit that should have been a patch. (The verify harness and a clean-tree
assertion
catch both before they land.)

---

## The build driver: a wired pipeline, not a list of steps

The build is a composition of functions in which **each edge guarantees the
precondition the next stage relies on.** Nobody "performs the steps"; the driver
runs the graph and the invariants hold by construction. The wiring, in full:

```bash
source patches/patches.sh

reset_source_trees                       # ⟶ guarantees: source == upstream HEAD
audit_patches "$(cat .upstream-lock)" \
              "$(git -C "$SRC" rev-parse HEAD)"   # ⟶ on update: classify/flag drift
apply_patches_prebuild                   # ⟶ guarantees: customizations present
trap 'unapply_patches_prebuild' EXIT     # ⟶ guarantees: revert happens NO MATTER WHAT
build                                    # compile the customized tree
install_to_staging                       # binaries land in staging, not live
apply_patches_install                    # ⟶ guarantees: staging is set up
trap - EXIT                              # success path: disarm the net...
unapply_patches_prebuild                 # ...and revert explicitly ⟶ source pristine
git -C "$SRC" rev-parse HEAD > .upstream-lock   # ⟶ baseline advances ONLY on success
```

Each wire, and the guarantee it makes deterministic:

- **`reset_source_trees` first, unconditionally.** Hard-resets the clone and
  every
  submodule to HEAD. On a clean tree it is a no-op; on a dirty tree it discards
  residue from a build killed in a way that bypassed the trap (SIGKILL, OOM,
  power
  loss). Running it always — never "only if dirty" — is what makes "source
  ==
  HEAD" a *guaranteed* entry precondition rather than a hope. It is the backstop
  for the one failure the trap cannot catch: a signal that kills the shell
  outright.

  ```bash
  reset_source_trees() {
      git -C "${SRC}" reset --hard HEAD --quiet
      for sub in "${SRC}"/<submodules>/*/; do
          [[ -d "${sub}/.git" ]] && git -C "${sub}" reset --hard HEAD --quiet
      done
  }
  ```

- **The trap is the revert machine.** `trap 'unapply_patches_prebuild' EXIT`
  wires
  the revert to process exit, so a crashed or failed build leaves the source
  clean
  anyway. The explicit `unapply` on the success path (after disarming) is the
  normal route; the trap covers every abnormal one. Together they make "the tree
  returns to HEAD" true across *all* exit modes, with no operator action.

- **`.upstream-lock` advances only after success.** Wiring the lock write to the
  end of the successful path means the audit's baseline can never point at a
  version you didn't actually ship. A failed build leaves the previous baseline
  intact.

The driver loads the active profile's patch list and dispatches by naming
convention — no central registry of "which function to call," just a glob and
a
grep over declared functions:

```bash
apply_patches_prebuild() {
    local patches="${PREBUILD_PATCHES[$PROFILE]:-}"
    for patch_id in ${patches}; do                  # e.g. "P001 P004 P009"
        local fn=$(declare -F | grep "^declare -f patch_${patch_id}_" | sed 's/declare -f //')
        [[ -n "${fn}" ]] && ${fn}
    done
}
```

`redownload-source` is the regeneration lever: it removes only the source tree
(leaving build dirs, installed artifacts, data, config untouched), so "the
source
is in a weird state" is never a debugging session — it's `redownload-source &&
install`. That shortcut is *safe specifically because* the real work lives in
`patches/`, not in the tree.

---

## Anatomy of a patch (the standard component)

Every patch file is a self-contained apply/unapply pair with a header. The
header
fields are not prose decoration — they are the input the generator emits and
the
data the registry generator and audit machine read back. Canonical shape:

```bash
#!/usr/bin/env bash
# P017-<short-name>.sh
#
# TARGET:   <relative/path/to/file> :: <function or region>
# SYMPTOM:  <what upstream does / the code as shipped>
# CHANGE:   <what we make it do, and WHY>
# ANCHOR:   <the unique pattern the apply keys on>
# WITNESS:  <a string present ONLY in the patched form>
# MATCHES:  1            # expected match count of ANCHOR (verify asserts this)

# {{{ patch_P017_<short_name>
patch_P017_<short_name>() {
    local FILE="${SRC}/<relative/path/to/file>"
    [[ -f "${FILE}" ]] || return 0                 # missing target = silent skip

    # Marker-wrapped replacement: the NEW block carries unique markers so the
    # inverse can locate it EXACTLY and can never false-match upstream or a
    # sibling patch, no matter how upstream reflows around it.
    sed -i -z 's|<OLD upstream text, escaped>|<C> {{{ P017-marker\n<NEW text>\n<C> }}} P017-marker|' "${FILE}"
}
# }}}

# {{{ unpatch_P017_<short_name>
unpatch_P017_<short_name>() {
    local FILE="${SRC}/<relative/path/to/file>"
    [[ -f "${FILE}" ]] || return 0
    # Exact inverse: match the marker-wrapped NEW block, restore OLD.
    sed -i -z 's|<C> {{{ P017-marker\n<NEW text escaped>\n<C> }}} P017-marker|<OLD upstream text>|' "${FILE}"
}
# }}}

# {{{ patch_needs_applying_P017   (the witness probe)
patch_needs_applying_P017() {
    local FILE="${SRC}/<relative/path/to/file>"
    [[ -f "${FILE}" ]] && ! grep -q "P017-marker" "${FILE}"   # absent witness ⟶ needs applying
}
# }}}
```

(`<C>` is the target language's line-comment token — `//`, `#`, `--`, `;`,
…)

The mechanical properties the *shape* enforces:

- **The guard makes idempotency structural.** A one-line edit guards with
  `grep -q "<unpatched anchor>" && sed …`; run it again and the anchor is
  gone, so
  nothing happens. The operator never has to track "did I already apply this."
- **Anchored, one-to-one patterns.** N problems = N anchored edits + N inverses.
  A
  broad pattern corrupts clean source; the `MATCHES` field plus the verify
  harness
  turn "anchor tightly" from advice into an asserted invariant.
- **Markers make the inverse robust.** A unique `{{{ <ID> … }}} <ID>` wrapper
  is a
  stable handle the inverse always finds and a sibling patch can never hit. The
  same marker doubles as the witness, so the probe and the inverse agree by
  construction.
- **The witness probe is the deterministic answer to "is this needed?"** —
  reused
  by the build's skip shortcut, by `--dry-run`, and by the audit's convergence
  test.

## The generator and the verifier replace the "add a patch" checklist

Adding a patch is not a remembered procedure; it is **a generator that stamps
the
component and a verifier that refuses malformed ones.** The only human input is
the irreducible creative part — the actual old→new edit and its anchor.

- **`scaffold-patch <tier> <name> <file>`** computes the next free ID for the
  tier
  (by scanning existing filenames), emits the skeleton above with the header
  fields and the `patch_/unpatch_/needs_applying_` triplet pre-named, and
  pre-fills
  the marker string. The author fills in only the `sed` bodies and the
  OLD/NEW/ANCHOR/WITNESS fields.
- **`verify-patches`** is the assertion harness, wired into the build and CI as
  a
  gate. For each patch it mechanically checks, against a fresh clone:
  - apply changes the tree;
  - `grep -c "<ANCHOR>"` equals the declared `MATCHES` (0 or >1 ⟶ fail);
  - the witness is present after apply and absent after unapply;
  - **the round-trip is exact**: `apply → unapply → git diff` is empty.
  A patch that fails any assertion never enters the active set — the property
  isn't "the author tested it," it's "the harness won't let a broken one build."
- **`gen-registry`** regenerates `docs/patches/patch-registry.md` by reading the
  header fields of every patch file. The registry is a *generated artifact*: it
  cannot drift from reality because it is never hand-edited, only recomputed.
  "Add
  a row to the registry" — a classic forgettable step — ceases to exist as a
  manual act.

---

## The orchestrator (`patches.sh`): wiring, not bookkeeping

One sourced file wires the components together:

1. **Auto-registration by glob.** Each patch file, when sourced, registers its
   functions; the orchestrator just globs them in. Adding a file *is*
   registering
   it — there is no separate manifest to keep in sync.
   ```bash
   PATCHES_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
   for f in "${PATCHES_DIR}"/P[0-9][0-9][0-9]-*.sh; do [[ -f "$f" ]] && source "$f"; done
   ```

2. **Per-profile selection as declarative structure.** The one genuinely
   *declarative* input — which patches a build variant wants — lives as an
   associative array (configuration in structure, greppable, with inline
   justification). This is the human's intent encoded as data the drivers read;
   it
   is config, not procedure.
   ```bash
   declare -A PREBUILD_PATCHES=(
       ["minimal"]="P001 P004"
       ["standard"]="P001 P004 P009 ... P024"   # compile fixes + module compat
       ["full"]="P001 P002 P003 ... P024"       # everything incl. feature hooks
   )
   ```

3. **The drivers** (`apply_*`, `unapply_*`, `reset_source_trees`,
   `patches_need_applying`, `audit_patches`) each iterate the active profile's
   list
   and dispatch by naming convention. Dispatch-by-convention means there is no
   lookup table to maintain — the function name *is* the registration.

**Revert reporting is computed, not narrated.** `unapply_patches_prebuild`
snapshots `git status --porcelain` across the tree and submodules before and
after
each unpatch and prints `[<ID>] Reverted` only when content actually changed.
"Nothing printed" is therefore an unambiguous, machine-derived fact (with a
no-op
count in the summary), not an operator guess.

---

## Profiles: one parts bin, many machines

A **profile** is a named selection of patches (and submodules, and config
values).
The same `patches/` directory drives every variant; the per-profile arrays
select
the subset. The active profile is itself read mechanically — from a `.profile`
file, overridable by `--profile` — so the selection is data the machine
consumes,
never a flag an operator must remember to pass:

```bash
PROFILE="$([[ -f "${ROOT}/.profile" ]] && cat "${ROOT}/.profile" || echo "standard")"
```

The **a/b variant pattern** encodes "same setting, different value per profile"
as
two sibling patches with non-overlapping profile lists, so exactly one fires.
History stays visible — old values are recorded as siblings, not overwritten
—
which is itself a small machine for never losing the prior decision.

---

## De-patching: three cleanup mechanisms, each wired to its own trigger

There is no "decide which cleanup to run." There are three mechanisms, and the
**call site selects the right one automatically**:

| Mechanism | Wired to (trigger) | Guarantee it provides |
|-----------|--------------------|-----------------------|
| `unapply_patches_prebuild` | the EXIT trap, and the success-path revert | reverses exactly the patches *this run* applied, with computed per-patch reporting |
| `reset_source_trees` | the unconditional pre-flight at every build entry point | drives the tree to a known commit; cannot fabricate dirt |
| `apply-patches --revert` | the operator surface (manual) | exposes revert by hand; for source patches it also triggers an incremental rebuild so the binary matches |

The split exists because of a real failure mode that makes one mechanism unsafe
in
the other's role:

> An `unapply` transformation targets *the post-patch shape*. It cannot
> distinguish
> "we applied this" from "upstream now ships this." When upstream converges on
> the
> form you patched to, a defensive unapply silently reverts **clean upstream
> code
> into false dirt.**

So the wiring assigns each mechanism the job it is safe for: `unapply` only ever
runs to reverse patches the same run applied (where authorship is certain), and
`reset_source_trees` — which goes to a commit and *cannot* manufacture dirt
— is
the one wired to every defensive/pre-flight position. The operator never
chooses;
the call site already did.

The operator surface itself is the one deliberately human-facing tool:

```
./scripts/apply-patches --dry-run            # show which patches WOULD apply (computed)
./scripts/apply-patches --prebuild           # apply source patches to the tree
./scripts/apply-patches --prebuild --revert  # revert + incremental rebuild
./scripts/apply-patches --profile minimal --prebuild
```

It is a thin, explicit control panel over the same drivers — not a procedure,
a
switchboard.

---

## The pruning machine: auditing patches on every upstream update

Patches rot as upstream moves: some become redundant (upstream fixed it), some
stale (upstream restructured the target), some quietly mean something different
(upstream changed the region around your anchor). The pruning machine makes "are
these still needed?" a **computed verdict wired into the update path** — not a
review someone must remember to run. It is invoked by the build driver on every
update (`audit_patches OLD NEW`), so it *cannot be skipped*.

## What it computes, per patch, against the pristine new tree

The machine reads each patch's `TARGET`, `ANCHOR`, and `WITNESS` header fields
and
emits exactly one verdict:

1. **RETIRE (convergence).** The witness probe reports the patch is no longer
   needed on pristine upstream → upstream adopted your shape. The edit is
   redundant.
2. **STALE (disappearance).** The anchor is absent from the target → upstream
   restructured the code out from under the patch; apply can no longer match.
3. **REVIEW (collision).** Upstream's changed line ranges overlap the patch's
   anchor line → the patch may still apply but its meaning in the new context
   is no
   longer guaranteed.
4. **OK.** Anchor intact, region untouched.

The collision test is computed directly from the version-control delta — hunk
headers give the changed line ranges in the new revision:

```bash
# Lines changed in <file> between old and new upstream. --unified=0 makes each
# hunk header report exactly the changed range: @@ -<oS>,<oL> +<nStart>,<nLen> @@
git -C "${SRC}" diff --unified=0 "${OLD_REF}" "${NEW_REF}" -- "${file}" \
  | awk '/^@@/ { split($3,a,","); s=substr(a[1],2)+0; l=(a[2]==""?1:a[2])+0;
                 if (l>0) print s","(s+l-1) }'
```

The patch's anchor line is `grep -n "<ANCHOR>" "${file}" | head -1 | cut -d:
-f1`;
overlap with any printed range (optionally padded by context) is the trigger.

## The machine emits its own worklist — and a durable record

For every REVIEW and STALE verdict, the audit **auto-inserts a uniquely-tagged
comment** into the working tree directly above the anchor, in the file's own
comment syntax:

```bash
insert_audit_tag() {                       # <file> <line> <patch_id> <range>
    local C; C="$(comment_token_for "$1")" # //, #, --, ; … by extension
    sed -i "${2}i\\${C} [[PATCH-AUDIT $3 $4]] upstream changed this region — \
confirm patch still needed and the anchor still matches, then delete this line." "$1"
}
```

Now a single `grep -rn "PATCH-AUDIT" "${SRC}"` *enumerates the entire decision
set*
mechanically — the machine produces the worklist; the human (or LLM) supplies
only
the irreducible judgment per site, then deletes the tag. Two records, two
lifetimes, both produced automatically:

- **In-tree tags are ephemeral** — they live in the disposable tree and vanish
  at
  the next `reset_source_trees`, which is exactly right for one review session.
- **A persistent report** (`docs/patches/audit-<NEW_REF>.md`) records every
  verdict, so the decision history survives the reset.

## The gate

Whether the audit *blocks* or merely *warns* is a wired policy, not a habit:
block
in CI/releases (a stale patch is a latent bug), warn in fast local iteration.
The
`.upstream-lock` baseline advances only on a successful build, so the next audit
always compares against the last *known-good* version — the machine cannot
drift
its own reference point.

---

## Patch docs: the prose form (a portable replay artifact)

For complex multi-file changes, a **prose "patch doc"** in
`docs/patches/<feature>.md`
describes the edit in human/agent-readable form — file paths, insertion points
by
*context* (not just line number), before/after snippets — so any agent or
human
can **replay it onto any compatible upstream** even after line numbers drift. It
is
the durable, forge-independent specification a script (or a reviewer)
implements.

```markdown
# <Feature> Patch
## Overview        — what this adds and why
## Files to Modify — for each file: context to locate the spot + the change
## Build           — how to rebuild after applying
```

The three artifacts form a ladder: a **behavior spec** says what the system
should
do (portable across reimplementations); a **patch doc** says what edits to make
to
one codebase (portable across upstreams of it); a **patch script** is the
mechanical, idempotent, reversible implementation. Each is derivable downward.

---

## Contributing a patch upstream: the same machine, run forward-only

A subset of your patches are genuine upstream improvements. For those, the
system
is already most of a contribution machine: the build runs `apply → build →
unapply`; a contribution runs `apply → commit`. **Same apply function,
different
downstream wiring.** A patch's anchored transformation against a pristine clone
of
upstream HEAD *produces the contribution diff*; the header *is* the PR body.

This is built as a driver with **gates** at the outward-facing edges — the one
place where the machine deliberately halts and requires irreducible human input.

## The two human inputs the machine is built around

- **Eligibility is a human judgment, never automated.** Only the author knows
  whether an edit belongs upstream. The machine therefore records no
  upstreamability field and decides nothing; it acts only on a patch a human
  hands
  it. (A behavior change worth upstreaming should be contributed behind a config
  flag, with the patch-doc as the PR body.)
- **Each outward action is gated on approval.** Fork creation, branch push, PR
  open, and optional fork deletion each pass through a gate that renders what
  will
  happen and refuses to proceed without an explicit yes.

Everything *between* those two human touch-points is wired and deterministic:

## Forge detection — computed from the remote, not asked

```bash
remote_url="$(git -C "${SRC}" remote get-url upstream 2>/dev/null \
            || git -C "${SRC}" remote get-url origin)"
case "${remote_url}" in
    *github.com*) FORGE=gh   ;;   # gh pr create
    *gitlab*)     FORGE=glab ;;   # glab mr create
    *gitea*|*)    FORGE=tea  ;;   # gitea: tea pr create / REST API
esac
# owner/repo derive from the same URL; an unrecognized host trips a gate (ask).
```

## Fork and clone are different objects with different lifetimes

The machine keeps these roles mechanically distinct, which is what lets a
persistent fork coexist with a throwaway tmpfs clone:

- **The fork is server-side and durable.** Created once on the account, it holds
  the PR branches and survives so review threads persist. It is **not** on
  tmpfs.
- **The working clone is local and disposable, and exactly one is needed** for
  any
  number of open PRs. Per patch, the machine resets the clone to pristine
  default-branch HEAD (reusing `reset_source_trees`), cuts `patch/<id>-<name>`,
  applies that one patch forward-only, commits, and pushes the branch to the
  fork.
  The fork accumulates one branch per PR; the clone is recycled between them.
- **Each PR carries only its relevant files, by construction** — because each
  patch is atomic and one-to-one targeted, its branch differs from base only in
  the
  files it touches. No filtering machinery is needed.
- **Open PRs survive clone deletion**: once a branch is pushed, its commits live
  on
  the remote fork, so end-of-run cleanup (`rm` the clone) cannot disturb them.

## Clone placement — a measured decision, with a logged fallback

The clone prefers the RAM-backed `tmp/` symlink and falls to on-disk only when
it
won't fit. The fit test measures **space available on that mount right now**
(not
total capacity — other things already occupy it):

```bash
avail=$(df -kP "${ROOT}/tmp" | awk 'NR==2 {print $4 * 1024}')   # free bytes NOW
need=$(( $(forge_repo_size_kb) * 1024 * 2 ))                    # shallow clone + margin
if (( avail >= need )); then
    CLONE_DIR="${ROOT}/tmp/pr-work"
else
    CLONE_DIR="${ROOT}/.pr-work"        # on-disk fallback inside the project
    echo "FALLBACK: tmp/ has ${avail} B free; clone needs ~${need} B. Using" \
         "on-disk ${CLONE_DIR}. This is a fallback, not the normal path." >&2
fi
git clone --depth 1 --branch "<default-branch>" "<fork-url>" "${CLONE_DIR}"
```

The fallback announces itself every time it fires — a silent fallback would
hide a
capacity problem worth surfacing. Either location is `rm`'d when the run ends.

## The verify-match interlock

Before any gate is reached, the machine asserts each patch's anchored pattern
hits
its declared `MATCHES` count on upstream HEAD. A zero or extra match means the
patch was written against an older ref and **the driver aborts** rather than
opening a PR on stale assumptions — the same signal the pruning machine uses,
reused here as a pre-flight interlock.

## The driver, as a graph

```
select(patch)            ─ human: hands the machine an eligible patch
detect_forge(remote)     ─ computed
ensure_fork()            ─ GATE: approve creating the server-side fork (once)
place_clone()            ─ tmpfs if it fits now, else on-disk fallback (logged)
for each patch:
    reset → branch → apply(forward-only) → verify_match()   ─ abort on mismatch
    render(diff, pr_body_from_header)                       ─ computed
    GATE: approve push + PR open
    push_branch(); open_pr(FORGE)
cleanup()                ─ rm the clone; fork + open PRs remain
```

The driver *prepares* everything up to each gate and stops; nothing reaches the
network or the account without the human passing that gate.

---

## Non-textual patches (same discipline, different substrate)

The apply/unapply/idempotent/round-trip machinery is not specific to text:

- **A data row** (registering a path with an upstream's own migration/plugin
  runner): apply is an idempotent insert (`INSERT IGNORE`, or
  create-if-not-exists
  keyed on a unique column); unapply is the matching delete; the guard checks
  for
  the row.
- **A binary asset or generated file**: apply writes/links it, unapply removes
  it,
  the guard checks presence.

**Where upstream provides its own seam — a plugin dir, a migrations folder, a
hooks
directory — wire into it instead of editing upstream files.** A registration
is a
patch that *cannot* conflict, because you are using a mechanism upstream
intends.

---

## Assembling the machine in a new project

This is a parts list and wiring diagram, not a sequence to march through. Build
the
components; the guarantees emerge from how they connect.

- **Disposability wire:** add `source/` (or `vendor/`, or the submodule path) to
  `.gitignore`. Commit only `patches/` and `scripts/`. This single line is what
  makes the tree a regenerable artifact rather than a thing to protect.
- **The orchestrator (`patches.sh`):** glob-source patch files; declare the
  per-profile `declare -A` lists; define `reset_source_trees`, the
  `apply_/unapply_`
  drivers, and `audit_patches`. Dispatch by naming convention so there is no
  manifest to maintain.
- **The standard component:** patches as guarded, anchored, marker-wrapped,
  exact-inverse pairs with a witness probe and header fields. The substrate (any
  language, any config DSL) is irrelevant — anchored `sed`/`awk` operate on
  any
  text tree; non-textual patches swap the tool and keep the contract.
- **The generators and the gate:** `scaffold-patch` (emits components),
  `gen-registry` (recomputes the registry from headers), `verify-patches`
  (asserts
  round-trip + match-count and blocks the build on failure).
- **The build driver:** compose `reset → audit → apply → (trap) → build
  → install →
  unapply → record-lock` exactly as the wiring above, so every precondition is
  a
  guaranteed product of the prior edge.
- **The pruning machine + lock:** `audit_patches` wired into the update path,
  `.upstream-lock` written only on success.
- **The contribution driver (optional):** forward-only apply against a fork
  clone,
  forge detection from the remote, verify-match interlock, and approval gates at
  the outward edges.

Tiers beyond PRE-BUILD are added only when the chronology demands them.

---

## Failure modes the machinery is built to foreclose

These are not reminders for an operator — they are the specific failures each
mechanism above exists to make impossible. Stated as "the bug, and the part that
forecloses it":

- **False dirt from defensive unapply.** `unapply` matches the post-patch shape
  and
  cannot tell your edit from an upstream that converged on it; run defensively
  it
  reverts clean code into phantom diffs that read like upstream regressions.
  *Foreclosed by* wiring `unapply` only to same-run reverts and
  `reset_source_trees`
  (which goes to a commit) to every defensive position — the call site, not
  the
  operator, picks the safe one.
- **Silent corruption from a loose pattern.** An unanchored pattern matching two
  sites edits the wrong one. *Foreclosed by* the `MATCHES` field +
  `verify-patches`,
  which fail any patch whose anchor count isn't exactly as declared.
- **A fragile inverse.** Reverting by re-matching content breaks when upstream
  reflows around the block. *Foreclosed by* the unique `{{{ <ID> }}}` markers,
  which give the inverse a stable handle and double as the witness.
- **Double-apply / double-revert.** Retries, re-sourcing, and trap-over-explicit
  all run things twice. *Foreclosed by* the guards that make a repeated apply or
  unapply inert.
- **A patch that applies but won't reverse.** It leaks false dirt into the
  *next*
  build, far from its origin. *Foreclosed by* the round-trip assertion in
  `verify-patches` (`apply → unapply → empty diff`), which blocks it from
  the set.
- **Residue from an uncatchable kill.** The EXIT trap misses SIGKILL/OOM/power
  loss. *Foreclosed by* the unconditional pre-flight `reset_source_trees`, which
  covers exactly the modes the trap cannot.
- **A re-targeting need going unnoticed.** Upstream moves the code under a
  patch.
  *Foreclosed by* a loud unmatched-pattern failure and the pruning machine's
  STALE/REVIEW verdicts with auto-inserted tags — the machine surfaces the one
  patch needing attention instead of letting it drift.
- **The patch set silently rotting.** *Foreclosed by* wiring `audit_patches`
  into
  the update path so it cannot be skipped, and generating the registry from
  headers
  so it cannot go stale.
- **A poisoned audit baseline.** *Foreclosed by* writing `.upstream-lock` only
  on a
  successful build, so the machine never compares against a version it didn't
  ship.
- **A tuning knob masquerading as a build failure.** *Foreclosed by* the
  chronology: POST-DEPLOY writes runtime values only after the validation gate.
- **A tracked or surviving source edit.** It breaks the `upstream + patches`
  guarantee. *Foreclosed by* the `.gitignore` wire plus a clean-tree assertion
  —
  any surviving edit is a verify failure, not a thing to remember not to do.
- **An outward action firing without consent.** *Foreclosed by* the approval
  gates
  on fork/push/PR/delete, which render the action and refuse to proceed without
  an
  explicit yes.

--------------------------------------------------------------------------------

### Assistant Response 6

> Now I have the machinery. The patch auto-registers by glob, so this needs no
> edit to `patches/E-patches.sh` either — and the runner treats a non-zero
> return as a hard failure, which lets me guard properly instead of letting a
> `sed` miss silently.

> Let me test the patch against a copy of the real config before it goes
> anywhere near an install.

> All ten pass. Let me verify the runner derives the expected function name from
> the filename, since that drives the profile listing.

> Matches. Now recording the implementation in 148w.

> Now stopping MySQL as you asked.

148w is fixed and committed as `cf62302`. MySQL is stopped (the unrelated system
instance at `/mnt/mtwo/programs/mysql-server` is still up — that one was
already running when I got here and isn't the project's).

**The patch.** `config/patches/C022-vanilla-custom-spells.sh`, gated on vanilla,
setting `PlayerStart.CustomSpells = 1`. No edit to `patches/E-patches.sh` was
needed — `apply_config_values()` globs `C*.sh`, sources each, and dispatches
over every declared `config_*` function, so dropping the file in registers it.
That also kept it clear of your in-flight work in that file. I did read the
patch-system skill first, per CLAUDE.md.

**One thing I did differently from its siblings, deliberately.** The existing
`C*.sh` patches rewrite their key with a bare `sed -i 's|^Key.*=.*|Key =
value|'`. That substitution cannot create a line that doesn't exist — so if
upstream ever renames or drops the key, the sed matches nothing, the patch
prints `[✓]`, and the config silently keeps its default. That silent-default
outcome is *exactly* the bug this issue is about, so C022 refuses to fail that
way: it checks the file exists, checks the key is present before writing, and
reads the value back afterward. Each failure returns non-zero, which the runner
surfaces as `[✗] ... FAILED` and propagates. The error text names
`CONFIG_START_CUSTOM_SPELLS` in `WorldConfig.cpp` as where to look if upstream
moved the key, and each message says what completed before the failure as well
as what didn't.

I've noted in the issue that retrofitting that guard onto the other twenty-one
config patches is worth doing, because any of them could be sitting on this same
defect right now and nobody would know.

**Tested** against copies of the real vanilla `worldserver.conf` — ten
assertions, six cases, all passing: the 0→1 flip, idempotent re-apply, exactly
one line in the file changing and nothing else disturbed, both guard paths
returning 1 with the right message, and duplicate key lines all converging to 1
rather than leaving a later one to win at parse time. The test script is in the
scratchpad, not the repo.

**To verify on your side:** reinstall so the config regenerates, restart, roll a
Blood Elf Mage, and count `character_spell` — expect 123. Characters that
already exist stay empty; the table is read at creation and never revisited, so
the 23,462-character bot fleet needs regenerating or the login-time top-up hook
that's still an open question in the issue.

Two things still queued from earlier: the 148v reversal also needs a reinstall
plus a cold-cache reroll to confirm, and both of those verifications want the
same server session — so they can be checked together.

--------------------------------------------------------------------------------

### User Request 7

okay what are the other changes that are active in the repo?

--------------------------------------------------------------------------------

### Assistant Response 7

> CLAUDE.md's change is a documentation restructure — and it pairs with the
> untracked `notes/claude-md-asides.md`, which preserves the exact lines
> removed. Let me look at the code changes.

Six distinct work-streams, all sitting uncommitted together. Here's what each
is.

**1. A playerbot crash investigation — issue 308, the largest thread.**
Untracked: `issues/308-playerbot-engine-init-stale-facade-crash.md`, a patch doc
at `docs/patches/playerbot-engine-init-facade-guard.md`, and two new source
patches — `B026` (2026-07-16, a bot-login crash guard plus diagnostic) and
`B027` (2026-07-21, an `Engine::Init` stale-facade guard, its companion).
`patches/patches.sh` registers both across all four profiles and adds a
`patch_needs_applying_B027` witness. It also drops `B010` everywhere, because
upstream now ships `ARENA_TYPE_NONE` itself.

The containment went further than patches: `scripts/install` and
`scripts/update` now **pin the shared source tree to commit `52f58186a533` and
mod-playerbots to `93aaea3d`** for beta, release, and vanilla — previously all
three tracked HEAD. The comment is explicit that moving HEAD would re-open the
diagnosis, and that both pins lift together once upstream fixes the corrupt
facade.

**2. A crash-backtrace harness.** `scripts/debug-run.sh` (untracked) runs a
binary under gdb, or falls back to an `LD_PRELOAD` shim plus `addr2line`.
`scripts/compile` gains `--debug` (sets `CMAKE_BUILD_TYPE=Debug`, implies
`--force` since the build type needs a reconfigure) and stamps
`bin/.build-type`. `scripts/worldserver` and `scripts/authserver` gain
`--debug`, and worldserver reads that stamp to enable the catcher automatically
— so a debug build always captures traces without anyone remembering the
runtime flag. Clearly built to serve stream 1.

**3. Config-pipeline correctness — three separate silent-failure bugs.**
- `patches/E-patches.sh` E006 now refreshes the app `.conf.dist` templates from
  source before the `.dist → .conf` copy, because the installed templates
  drifted stale: the binary started reading keys that weren't in the installed
  file, so the server logged "Missing property X" and fell back to defaults. The
  comment says this supersedes `C016`, which was written as a stopgap for the
  same thing. The 5,992-line churn in
  `installed-files-release/etc/worldserver.conf.dist` is the *result* —
  regenerated from current upstream, with the whole section index restructured.
- `C012` and `C014`: the sed `^AiPlayerbot.RandomBotMinLevel.*=.*` was greedy
  and also matched `RandomBotMinLevelChance = 0.1`, rewriting it into a
  duplicate level key **and** deleting the Chance key. Fixed by anchoring with
  `[[:space:]]*=`.
- `C011`: the realmlist update ran `WHERE id = 1`, leaving the
  beta/vanilla/alpha rows on `127.0.0.1` — so a remote player could
  authenticate but was handed loopback for the world server and couldn't get in.

**4. Bot population tuning.**
`config/patches/C020-vanilla-playerbot-population.sh` (untracked) bounds the
ambient fleet to 128–256 online bots, down from upstream's 500/500, with the
rationale in `docs/balance-updates.md`: 500 was more traffic than the 20–40
Eastern Kingdoms band needs and paid for it as a cold-boot login stampede every
startup.

**5. The CLAUDE.md restructure.** Adds the Profiles, Commit Conventions, and
Patch System sections; removes the asides — which is exactly what the
untracked `notes/claude-md-asides.md` preserves. Those two are one change and
should commit together.

**6. Two unrelated archives.** The `issues/declined/1101*` family (14 files —
the Bellwether attended-play design), plus declined `807` and a phase-11
progress doc. And separately the untracked `917` series, five files from
2026-08-01. Also `pngs/missing-the-point.png`.

**One thing worth pulling out.** Four of these are the same bug wearing
different clothes — *something in the patch pipeline silently did nothing and
nobody found out for weeks*:

- `B002` was retargeted because upstream moved `OnBotLoginInternal` out of the
  file it patched. The anchor vanished, the sed no-op'd silently, and **bots
  stopped firing the ALE login hook entirely**.
- `C012`/`C014`'s greedy sed matched the wrong line and deleted a key.
- E006's stale templates meant configs quietly ran on defaults.
- And 148w, which I fixed today: a config flag left at its default meant 717
  rows of pretrain data were never read, for months.

That's why I wrote the key-presence and read-back guards into C022 rather than
copying the sibling patches' bare substitution — and why I noted retrofitting
them onto the other twenty-one config patches as a step in 148w. The patch
skill's `MATCHES` count and witness-probe discipline is the same guard on the
source-patch side; `B027` has a witness, `B002` evidently didn't when it broke.

Nothing here is mine and none of it is staged. My three commits today
(`42a2c69`, `8b0ef7b`, `cf62302`) touched only files that were clean when I
started, which is why no line-level splitting was needed.

--------------------------------------------------------------------------------

### User Request 8

okay are any of them incomplete? If so, let's work on them. If not, let's commit
them, using the same standard procedure we always do - strip out just the
relevant changed lines, commit those, then restore the rest of changed work for
a different commit.

--------------------------------------------------------------------------------

### Assistant Response 8

> Let me assess completeness before committing anything.

> Issue 308 is explicitly in progress. Let me see what's actually left on it.

> Found a real gap — B027 has a witness probe, B026 doesn't. Let me check
> whether that's an anomaly or the norm.

> Confirmed — every B patch from B001 to B027 has a witness probe **except
> B026**, and the consumer silently skips patches that lack one. That's the same
> silent-hole class of bug this whole body of work has been fighting. Let me see
> what B026 injects so I can write its probe.

> Three concrete gaps. Let me confirm the third before fixing any.

> Confirmed — that patch doc doesn't exist and is referenced nowhere else. Let
> me check the remaining streams before I start fixing.

> All syntactically sound, and the 917 files are complete design documents. So:
> streams 2–6 are done; stream 1 is code-complete but blocked on a build you
> run — plus the three gaps. Let me close those. First, B002's actual witness
> string.

> The patch itself targets `PlayerbotMgr.cpp`; only its witness was left behind.
> Fixing both witnesses now.

> Coverage is now unbroken, B001 through B027. Now B026's missing patch doc.

> Now let me plan the commit split. First, checking whether any single file
> spans two streams.

> `playerbot-vanilla-starter-kit.md` was tracked but missing from the TOC, so
> that entry fixes a pre-existing gap too.
>
> One useful finding for the split: **no file this time spans two streams**, so
> the line-level `git apply --cached` step isn't needed — each modified file
> belongs wholly to one commit. The one that came close is `patches.sh`, where
> the B010 retirement and the B026/B027 registration are edits to the *same
> array lines*, so they're inseparable and travel together.

Working tree is clean. Seven commits for the pre-existing work, split by
concern.

**On the split mechanics:** the line-level `git apply --cached` step wasn't
needed this time — every modified file belonged wholly to one concern. The one
that came close was `patches.sh`, where retiring B010 and registering B026/B027
are edits to the *same array lines*, so they're physically inseparable and
travelled together in the playerbot commit. Splitting them would also have
produced a broken intermediate commit, with `patches.sh` naming patch files that
didn't exist yet.

**What was incomplete, and what I did about it.**

Issue 308 says "In progress (repair patch written, needs build + verification)"
and that's still true — steps 1 through 3 are done, step 4 is a build and a
login burst, which is yours to run. I didn't touch that.

But three things in that stream were incomplete in ways I could close, and all
three are the same failure this body of work has been fighting all along —
machinery that had quietly stopped reporting:

- **B002's witness probe was left pointing at the old file** when the patch
  itself was re-aimed at `PlayerbotMgr.cpp`. It was grepping
  `RandomPlayerbotMgr.cpp` for a marker that can never appear there, so it
  answered "needs applying" unconditionally, forever. A probe with only one
  possible answer can't detect the thing it exists for — which is exactly how
  B002 broke unnoticed in the first place. Re-aimed.
- **B026 shipped with no witness probe at all**, and `patches_need_applying()`
  silently skips any patch lacking one (`declare -F ... || skip`). So B026 never
  contributed to that check in its life. Every other patch from B001 to B027 had
  one; coverage is now unbroken.
- **B026's header pointed at a patch doc that didn't exist** —
  `playerbot-bot-login-strategy-guard.md`, referenced nowhere else in the tree.
  Written, covering the crash stack, the ownership argument for why
  delete-before-reconstruct is safe, both edits, the reversal, the witness, and
  how it composes with B027. Added it to the contents page along with B027's doc
  and `playerbot-vanilla-starter-kit.md`, which was tracked but had never been
  listed.

Everything else checked out: `debug-run.sh` is 285 lines and syntactically
clean, all five patch scripts pass `bash -n`, the 917 files are complete design
documents, and the declined material is complete by nature.

**Still outstanding, all needing a server you drive:** the 148v kit reversal and
the 148w spell fix both want a reinstall plus a reroll, and 308 wants a build
plus a login burst — those can share one session. And per 308's own note, the
diagnostic trail in the patch doc is enough to file upstream against
mod-playerbots whenever that's convenient.

--------------------------------------------------------------------------------

