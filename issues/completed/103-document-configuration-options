# 103: Document Configuration Options

## Status
- [ ] Open
- [ ] In Progress
- [x] Completed

## Current Behavior

Configuration files exist but are not documented. Users must read raw .conf files
or external AzerothCore documentation to understand available options.

Current configuration files:
- installed-files/etc/authserver.conf
- installed-files/etc/worldserver.conf
- installed-files/etc/mod_grownup.conf
- (possibly others in etc/modules/)

## Intended Behavior

A project-specific configuration reference document exists that:
1. Lists all modified configuration options with explanations
2. Documents custom paths and their purposes
3. Explains module-specific configurations
4. Provides examples for common customizations

## Suggested Implementation Steps

1. List all .conf files in etc/ directory
2. Extract non-default values from each config
3. Document purpose of each modified setting
4. Create docs/configuration.md with findings
5. Update table-of-contents.md to include new doc

## Template for Configuration Document

```markdown
# Configuration Reference

## Modified Settings

### authserver.conf
| Setting | Value | Purpose |
|---------|-------|---------|
| LoginDatabaseInfo | ... | Custom MySQL connection |

### worldserver.conf
| Setting | Value | Purpose |
|---------|-------|---------|
| DataDir | ... | Game data location |
```

## Related Documents

- docs/installation.md (Configuration Files section)
- docs/table-of-contents.md

## Notes

This is a documentation task, not a code task. Can be completed independently
of server startup verification.

## Dependencies

None - can proceed in parallel with other Phase 1 issues.

## Completion Notes (2026-01-29)

### What Was Done

Created comprehensive `docs/configuration.md` documenting:

1. **Path Configuration** - All custom paths for authserver and worldserver
2. **Database Configuration** - Connection strings, MySQL settings (port 3307)
3. **Game Settings** - 50+ modified worldserver.conf settings organized by category:
   - Movement and Combat (slower speeds, increased fall damage)
   - Experience and Progression (3x talents, 10x rest)
   - Economy (scaled vendor prices, 25x repair costs)
   - Death and Corpse (no rez sickness, instant corpse reclaim)
   - Creatures (no leash, increased assist radius)
   - Visibility (extended view distance, disabled sparkles)
   - Quests (show all levels, solo raid quests)
   - Instances (enter at any level, solo raids)
   - Cross-Faction (all interactions enabled)
   - Guilds (solo creation, multiple GMs)
   - PvP (reduced prep time, no deserter)
4. **Module Configuration** - Eluna settings and script paths
5. **Lua Script Configuration** - Game loop timings, distances, tempo
6. **File Locations** - Quick reference for all config files

### Files Created/Modified

- Created: `docs/configuration.md` (comprehensive reference)
- Updated: `docs/table-of-contents.md` (added configuration.md)

### Notes

- Discovered mod_grownup.conf does not exist (issue file was incorrect)
- Only module present is mod_eluna.conf
- Playerbots configuration not found in worldserver.conf (may be built-in or separate)
