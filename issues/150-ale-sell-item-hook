# 150 - ALE Sell Item Hook

## Status
- Created: 2026-04-04
- Implemented: 2026-04-05
- **PATCH NOT IN SOURCE** (discovered 2026-04-09)
- Phase: 2
- Priority: Medium
- Type: C++ Patch

**Note (2026-04-09):** Issue was marked "Implemented" but the C++ changes are NOT in the source.
The patch documentation exists but was never added to `scripts/azerothcore` patcher.
Source was likely reset/re-cloned and changes were lost. See issue 334 for re-implementation plan.

## Current Behavior
- When player/bot sells item to vendor, transaction happens in C++ core
- No event emitted to Lua
- Lua scripts cannot react to vendor sales

## Intended Behavior
- New player event: `PLAYER_EVENT_ON_SELL_ITEM`
- Signature: `(event, player, item, vendor, count)`
- Fires BEFORE item is destroyed, allowing Lua to capture item data
- Enables treasure pool recycling, economy tracking, etc.

## Implementation Location

### Option A: Patch mod-ale (preferred)
File: `source-beta/modules/mod-ale/src/LuaEngine/PlayerHooks.cpp`

Add hook in the player sell item handler. ALE already hooks many player events,
this follows the same pattern.

### Option B: Patch AzerothCore core
File: `source-beta/src/server/game/Handlers/ItemHandler.cpp`
Function: `WorldSession::HandleSellItemOpcode`

Less preferred - modifying core means merge conflicts on updates.

## Technical Notes

### Existing Pattern in ALE
```cpp
// Example from PlayerHooks.cpp
void Eluna::OnLootItem(Player* player, Item* item, uint32 count, ObjectGuid lootguid)
{
    // ... calls Lua event
}
```

### New Hook (pseudocode)
```cpp
void Eluna::OnSellItem(Player* player, Item* item, Creature* vendor, uint32 count)
{
    if (!PlayerEventBindings->HasBindingsFor(PLAYER_EVENT_ON_SELL_ITEM))
        return;

    Push(player);
    Push(item);
    Push(vendor);
    Push(count);
    CallAllFunctions(PlayerEventBindings, PLAYER_EVENT_ON_SELL_ITEM);
}
```

### Event ID
Add to `ElunaEventMgr.h` or equivalent:
```cpp
PLAYER_EVENT_ON_SELL_ITEM = XX,  // (event, player, item, vendor, count)
```

## Benefits
- Treasure pool can recycle sold items
- Economy tracking possible
- Bot behavior can react to sales
- No client modification required

## Related Issues
- 149 - Sold items return to treasure pool (depends on this)
- 148 - Treasure chest shared loot

## Implementation Notes (2026-04-05)

### Files Modified

**ALE (modules/mod-ale/):**
- `src/LuaEngine/Hooks.h` - Added `PLAYER_EVENT_ON_SELL_ITEM = 74`
- `src/LuaEngine/LuaEngine.h` - Added `OnSellItem()` declaration
- `src/LuaEngine/hooks/PlayerHooks.cpp` - Implemented `OnSellItem()` hook

**AzerothCore Core:**
- `src/server/game/Handlers/ItemHandler.cpp` - Added `#ifdef ALE` include and hook calls

### Hook Call Sites
Two locations in `HandleSellItemOpcode`:
1. Line 728: Partial stack sale (`pNewItem`, `packet.Count`)
2. Line 740: Full item sale (`pItem`, `pItem->GetCount()`)

### Build Required
Requires full rebuild with `-DSCRIPTS=static -DMODULES=static`:
```bash
cd build && cmake .. && make -j$(nproc) && make install
```

### Patch Doc
Full patch specification: `docs/patches/ale-sell-item-hook.md`
