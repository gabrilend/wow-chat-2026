1. A "living" tag the user can attach under a "to" to update anyone above that line when the file chamges. Everyone else gets the default.

2. A LUA function that takes in a way point and a direction and returns the next way point in a chain. First, find the next point along a road / ambush route (path-chain agnostic) (manually defined by walking along the trail and entering points to a text file or dumping with a script to a text file from a LUA ability in-game) (start-new-trail and delete last point and make new point) anyway returns a point when given a point and a direction.

3. Make email android not move the body text when the "can't connect" or other warnings appear. Nothing should bother you while writing.

4. At crossroads have a function that returns a direction that the bot wants to walk down. Provide unit groups and filtering, keyed to integer indexes. What does that mean? Well, add a function that sorts a unit into an index. Attach text labels to the index, use Ollama to decide based on the text labels on a unit (players can type them at spawn when equip ping the unit with items they enchanted or forged) (of the tiger makes you prowl, of the bear makes you fierce, etc. Armor makes you fighty-er while robes make you want to study. This crossroads got magic left ways and combat to the right. At crossroads, there's 3 magic down this route and 5 down that route, I could or could not normalize it if I want to compare by paths-per-node. Use Dominions paths to highlight preferences, make grunts go anyway patrolled and gather materials. They do this automatically, and n are spawned per minute. Users can help. Users can pass items to them that get taken home and put toward whoever wants to use them.